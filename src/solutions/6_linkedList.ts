import { DetailedProblemData } from '../leetcodeSolutions'

export const linkedListSolutions: Record<string, DetailedProblemData> = {
  "Reverse Linked List": {
    intuition: "Iteratively reverse next pointers using prev, curr, and next_node pointers in a single pass.",
    bottleneck: "Recursion takes O(n) call stack memory. Iterative pointer reversal runs in O(n) time and O(1) space.",
    keyInvariant: "prev points to the head of the already reversed prefix list.",
    constraints: ["0 <= Number of nodes <= 5000", "-5000 <= Node.val <= 5000"],
    examples: [{"input": "head = [1,2,3,4,5]", "output": "[5,4,3,2,1]", "explanation": "List reversed in-place."}],
    companies: ["Amazon", "Microsoft", "Google", "Meta", "Apple"],
    acceptanceRate: "75.4%",
    frequency: "99%",
    edgeCases: ["Empty list", "Single node list"],
    interviewTips: ["Show both iterative O(1) space and recursive solutions."],
    optimal: {
      title: "Iterative Three-Pointer In-Place Reversal",
      subtitle: "O(n) time and O(1) space in-place pointer reversal",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Each node visited once.",
      spaceComplexity: "O(1)",
      spaceComplexityDetail: "Pointer variables only.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Save curr.next, set curr.next=prev, advance prev and curr.",
      keySteps: ["1. prev = None, curr = head.", "2. While curr: nxt = curr.next; curr.next = prev; prev = curr; curr = nxt.", "3. Return prev."],
      code: {
        python: ["class Solution:", "    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        prev, curr = None, head", "        while curr:", "            nxt = curr.next", "            curr.next = prev", "            prev, curr = curr, nxt", "        return prev"],
        cpp: ["class Solution {", "public:", "    ListNode* reverseList(ListNode* head) {", "        ListNode *prev = nullptr, *curr = head;", "        while (curr) {", "            ListNode* nxt = curr->next;", "            curr->next = prev;", "            prev = curr; curr = nxt;", "        }", "        return prev;", "    }", "};"],
        java: ["class Solution {", "    public ListNode reverseList(ListNode head) {", "        ListNode prev = null, curr = head;", "        while (curr != null) {", "            ListNode nxt = curr.next;", "            curr.next = prev;", "            prev = curr; curr = nxt;", "        }", "        return prev;", "    }", "}"],
        typescript: ["function reverseList(head: ListNode | null): ListNode | null {", "  let prev: ListNode | null = null, curr = head;", "  while (curr) {", "    const nxt = curr.next;", "    curr.next = prev;", "    prev = curr; curr = nxt;", "  }", "  return prev;", "}"],
      },
    },
    bruteForce: {
      title: "Array Extraction Rebuild",
      subtitle: "O(n) time and O(n) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Collects values into array.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "Array of size n.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Collect values into array, reverse, and construct new list.",
      keySteps: ["1. Collect values.", "2. Rebuild list."],
      code: {
        python: ["# Brute Force: Collect all node values in array, manipulate, and reconstruct list", "class Solution:", "    def solveBrute(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        if not head: return None", "        vals = []", "        curr = head", "        while curr:", "            vals.append(curr.val)", "            curr = curr.next", "        dummy = ListNode(0)", "        curr = dummy", "        for v in vals:", "            curr.next = ListNode(v)", "            curr = curr.next", "        return dummy.next"],
        cpp: ["// Brute Force: Array conversion - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "public:", "    ListNode* solveBrute(ListNode* head) {", "        if (!head) return nullptr;", "        vector<int> vals;", "        ListNode* curr = head;", "        while (curr) { vals.push_back(curr->val); curr = curr->next; }", "        ListNode dummy(0); ListNode* tail = &dummy;", "        for (int v : vals) { tail->next = new ListNode(v); tail = tail->next; }", "        return dummy.next;", "    }", "};"],
        java: ["// Brute Force: List collection - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    public ListNode solveBrute(ListNode head) {", "        if (head == null) return null;", "        List<Integer> vals = new ArrayList<>();", "        ListNode curr = head;", "        while (curr != null) { vals.add(curr.val); curr = curr.next; }", "        ListNode dummy = new ListNode(0); ListNode tail = dummy;", "        for (int v : vals) { tail.next = new ListNode(v); tail = tail.next; }", "        return dummy.next;", "    }", "}"],
        typescript: ["// Brute Force: Array collection - O(n) Time | O(n) Space", "function solveBrute(head: ListNode | null): ListNode | null {", "  if (!head) return null;", "  const vals: number[] = [];", "  let curr: ListNode | null = head;", "  while (curr) { vals.push(curr.val); curr = curr.next; }", "  const dummy = new ListNode(0); let tail = dummy;", "  for (const v of vals) { tail.next = new ListNode(v); tail = tail.next; }", "  return dummy.next;", "}"]
      },
    },
  },
  "Reverse Linked List II": {
    intuition: "Use dummy node before head. Advance prev pointer left-1 times. For each subsequent node in sublist, pop and insert after prev.",
    bottleneck: "Multi-pass sublist extraction is error prone. Single-pass insertion reverses in O(n) time and O(1) space.",
    keyInvariant: "prev.next always points to the newly reversed head of the sublist.",
    constraints: ["1 <= n <= 500", "-500 <= Node.val <= 500", "1 <= left <= right <= n"],
    examples: [{"input": "head = [1,2,3,4,5], left = 2, right = 4", "output": "[1,4,3,2,5]", "explanation": "Sublist [2,3,4] reversed to [4,3,2]."}],
    companies: ["Amazon", "Microsoft", "Google"],
    acceptanceRate: "46.8%",
    frequency: "85%",
    edgeCases: ["left == 1 (reversing head)", "left == right"],
    interviewTips: ["Use dummy node dummy = ListNode(0, head) to seamlessly handle left = 1."],
    optimal: {
      title: "One-Pass In-Place Head-Insertion Reversal",
      subtitle: "O(n) time and O(1) space single pass",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Traverses at most right nodes.",
      spaceComplexity: "O(1)",
      spaceComplexityDetail: "Pointer variables only.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Position prev at left-1. In a loop of right-left steps, move curr.next directly after prev.",
      keySteps: ["1. dummy = ListNode(0, head), prev = dummy.", "2. Advance prev left-1 times. curr = prev.next.", "3. Loop right-left times: nxt = curr.next; curr.next = nxt.next; nxt.next = prev.next; prev.next = nxt.", "4. Return dummy.next."],
      code: {
        python: ["class Solution:", "    def reverseBetween(self, head: Optional[ListNode], left: int, right: int) -> Optional[ListNode]:", "        if not head or left == right: return head", "        dummy = ListNode(0, head)", "        prev = dummy", "        for _ in range(left - 1): prev = prev.next", "        curr = prev.next", "        for _ in range(right - left):", "            nxt = curr.next", "            curr.next = nxt.next", "            nxt.next = prev.next", "            prev.next = nxt", "        return dummy.next"],
        cpp: ["class Solution {", "public:", "    ListNode* reverseBetween(ListNode* head, int left, int right) {", "        if (!head || left == right) return head;", "        ListNode dummy(0, head);", "        ListNode* prev = &dummy;", "        for (int i = 0; i < left - 1; ++i) prev = prev->next;", "        ListNode* curr = prev->next;", "        for (int i = 0; i < right - left; ++i) {", "            ListNode* nxt = curr->next;", "            curr->next = nxt->next;", "            nxt->next = prev->next;", "            prev->next = nxt;", "        }", "        return dummy.next;", "    }", "};"],
        java: ["class Solution {", "    public ListNode reverseBetween(ListNode head, int left, int right) {", "        if (head == null || left == right) return head;", "        ListNode dummy = new ListNode(0, head);", "        ListNode prev = dummy;", "        for (int i = 0; i < left - 1; i++) prev = prev.next;", "        ListNode curr = prev.next;", "        for (int i = 0; i < right - left; i++) {", "            ListNode nxt = curr.next;", "            curr.next = nxt.next;", "            nxt.next = prev.next;", "            prev.next = nxt;", "        }", "        return dummy.next;", "    }", "}"],
        typescript: ["function reverseBetween(head: ListNode | null, left: number, right: number): ListNode | null {", "  if (!head || left === right) return head;", "  const dummy = new ListNode(0, head);", "  let prev = dummy;", "  for (let i = 0; i < left - 1; i++) prev = prev.next!;", "  let curr = prev.next!;", "  for (let i = 0; i < right - left; i++) {", "    const nxt = curr.next!;", "    curr.next = nxt.next;", "    nxt.next = prev.next;", "    prev.next = nxt;", "  }", "  return dummy.next;", "}"],
      },
    },
    bruteForce: {
      title: "Array Slice Reversal",
      subtitle: "O(n) time and O(n) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Extracts values into array.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "Array storage.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Extract values, reverse subslice, overwrite node values.",
      keySteps: ["1. Extract values.", "2. Reverse slice.", "3. Overwrite nodes."],
      code: {
        python: ["# Brute Force: Collect all node values in array, manipulate, and reconstruct list", "class Solution:", "    def solveBrute(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        if not head: return None", "        vals = []", "        curr = head", "        while curr:", "            vals.append(curr.val)", "            curr = curr.next", "        dummy = ListNode(0)", "        curr = dummy", "        for v in vals:", "            curr.next = ListNode(v)", "            curr = curr.next", "        return dummy.next"],
        cpp: ["// Brute Force: Array conversion - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "public:", "    ListNode* solveBrute(ListNode* head) {", "        if (!head) return nullptr;", "        vector<int> vals;", "        ListNode* curr = head;", "        while (curr) { vals.push_back(curr->val); curr = curr->next; }", "        ListNode dummy(0); ListNode* tail = &dummy;", "        for (int v : vals) { tail->next = new ListNode(v); tail = tail->next; }", "        return dummy.next;", "    }", "};"],
        java: ["// Brute Force: List collection - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    public ListNode solveBrute(ListNode head) {", "        if (head == null) return null;", "        List<Integer> vals = new ArrayList<>();", "        ListNode curr = head;", "        while (curr != null) { vals.add(curr.val); curr = curr.next; }", "        ListNode dummy = new ListNode(0); ListNode tail = dummy;", "        for (int v : vals) { tail.next = new ListNode(v); tail = tail.next; }", "        return dummy.next;", "    }", "}"],
        typescript: ["// Brute Force: Array collection - O(n) Time | O(n) Space", "function solveBrute(head: ListNode | null): ListNode | null {", "  if (!head) return null;", "  const vals: number[] = [];", "  let curr: ListNode | null = head;", "  while (curr) { vals.push(curr.val); curr = curr.next; }", "  const dummy = new ListNode(0); let tail = dummy;", "  for (const v of vals) { tail.next = new ListNode(v); tail = tail.next; }", "  return dummy.next;", "}"]
      },
    },
  },
  "Merge Two Sorted Lists": {
    intuition: "Use dummy node and tail pointer. Compare list1.val and list2.val, attach smaller to tail.next, advance. Attach remaining list.",
    bottleneck: "Allocating new nodes wastes space. In-place pointer rewiring runs in O(n + m) time and O(1) space.",
    keyInvariant: "tail always points to the end of the merged sorted prefix.",
    constraints: ["0 <= Number of nodes <= 50", "-100 <= Node.val <= 100"],
    examples: [{"input": "list1 = [1,2,4], list2 = [1,3,4]", "output": "[1,1,2,3,4,4]", "explanation": "Merged sorted list."}],
    companies: ["Amazon", "Apple", "Google", "Microsoft"],
    acceptanceRate: "63.8%",
    frequency: "98%",
    edgeCases: ["One or both lists empty"],
    interviewTips: ["Attach the remaining non-null list with tail.next = list1 or list2."],
    optimal: {
      title: "Two-Pointer In-Place Splicing",
      subtitle: "O(n + m) time and O(1) auxiliary space",
      timeComplexity: "O(n + m)",
      timeComplexityDetail: "Traverses both lists once.",
      spaceComplexity: "O(1)",
      spaceComplexityDetail: "Pointer references only.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Compare heads of both lists, attach smaller node to tail. Attach remainder after loop.",
      keySteps: ["1. dummy = ListNode(0), tail = dummy.", "2. While list1 and list2: compare and attach smaller.", "3. tail.next = list1 or list2.", "4. Return dummy.next."],
      code: {
        python: ["class Solution:", "    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:", "        dummy = ListNode(0)", "        tail = dummy", "        while list1 and list2:", "            if list1.val <= list2.val:", "                tail.next = list1; list1 = list1.next", "            else:", "                tail.next = list2; list2 = list2.next", "            tail = tail.next", "        tail.next = list1 if list1 else list2", "        return dummy.next"],
        cpp: ["class Solution {", "public:", "    ListNode* mergeTwoLists(ListNode* list1, ListNode* list2) {", "        ListNode dummy(0);", "        ListNode* tail = &dummy;", "        while (list1 && list2) {", "            if (list1->val <= list2->val) { tail->next = list1; list1 = list1->next; }", "            else { tail->next = list2; list2 = list2->next; }", "            tail = tail->next;", "        }", "        tail->next = list1 ? list1 : list2;", "        return dummy.next;", "    }", "};"],
        java: ["class Solution {", "    public ListNode mergeTwoLists(ListNode list1, ListNode list2) {", "        ListNode dummy = new ListNode(0);", "        ListNode tail = dummy;", "        while (list1 != null && list2 != null) {", "            if (list1.val <= list2.val) { tail.next = list1; list1 = list1.next; }", "            else { tail.next = list2; list2 = list2.next; }", "            tail = tail.next;", "        }", "        tail.next = list1 != null ? list1 : list2;", "        return dummy.next;", "    }", "}"],
        typescript: ["function mergeTwoLists(list1: ListNode | null, list2: ListNode | null): ListNode | null {", "  const dummy = new ListNode(0);", "  let tail = dummy;", "  while (list1 && list2) {", "    if (list1.val <= list2.val) { tail.next = list1; list1 = list1.next; }", "    else { tail.next = list2; list2 = list2.next; }", "    tail = tail.next;", "  }", "  tail.next = list1 ? list1 : list2;", "  return dummy.next;", "}"],
      },
    },
    bruteForce: {
      title: "Recursive Merge",
      subtitle: "O(n + m) time and stack space",
      timeComplexity: "O(n + m)",
      timeComplexityDetail: "Recursive call stack.",
      spaceComplexity: "O(n + m)",
      spaceComplexityDetail: "Call stack frames.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Recursively attach smaller head to merged rest.",
      keySteps: ["1. Base cases.", "2. Recursive call."],
      code: {
        python: ["# Brute Force: Collect all node values in array, manipulate, and reconstruct list", "class Solution:", "    def solveBrute(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        if not head: return None", "        vals = []", "        curr = head", "        while curr:", "            vals.append(curr.val)", "            curr = curr.next", "        dummy = ListNode(0)", "        curr = dummy", "        for v in vals:", "            curr.next = ListNode(v)", "            curr = curr.next", "        return dummy.next"],
        cpp: ["// Brute Force: Array conversion - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "public:", "    ListNode* solveBrute(ListNode* head) {", "        if (!head) return nullptr;", "        vector<int> vals;", "        ListNode* curr = head;", "        while (curr) { vals.push_back(curr->val); curr = curr->next; }", "        ListNode dummy(0); ListNode* tail = &dummy;", "        for (int v : vals) { tail->next = new ListNode(v); tail = tail->next; }", "        return dummy.next;", "    }", "};"],
        java: ["// Brute Force: List collection - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    public ListNode solveBrute(ListNode head) {", "        if (head == null) return null;", "        List<Integer> vals = new ArrayList<>();", "        ListNode curr = head;", "        while (curr != null) { vals.add(curr.val); curr = curr.next; }", "        ListNode dummy = new ListNode(0); ListNode tail = dummy;", "        for (int v : vals) { tail.next = new ListNode(v); tail = tail.next; }", "        return dummy.next;", "    }", "}"],
        typescript: ["// Brute Force: Array collection - O(n) Time | O(n) Space", "function solveBrute(head: ListNode | null): ListNode | null {", "  if (!head) return null;", "  const vals: number[] = [];", "  let curr: ListNode | null = head;", "  while (curr) { vals.push(curr.val); curr = curr.next; }", "  const dummy = new ListNode(0); let tail = dummy;", "  for (const v of vals) { tail.next = new ListNode(v); tail = tail.next; }", "  return dummy.next;", "}"]
      },
    },
  },
  "Linked List Cycle": {
    intuition: "Use Floyd Tortoise and Hare algorithm. Fast pointer moves 2 steps, slow moves 1 step. If cycle exists, relative speed of 1 node/step guarantees collision.",
    bottleneck: "Hash set uses O(n) space. Fast/slow pointers achieve O(1) space.",
    keyInvariant: "Pointers will collide if and only if a cycle exists.",
    constraints: ["0 <= Number of nodes <= 10^4", "-10^5 <= Node.val <= 10^5"],
    examples: [{"input": "head = [3,2,0,-4], pos = 1", "output": "true", "explanation": "Cycle exists."}],
    companies: ["Amazon", "Microsoft", "Google", "Meta"],
    acceptanceRate: "49.2%",
    frequency: "95%",
    edgeCases: ["Empty list", "Single node without cycle"],
    interviewTips: ["Explain relative velocity: distance between fast and slow decreases by 1 each step."],
    optimal: {
      title: "Floyd's Cycle Finding (Fast & Slow Pointers)",
      subtitle: "O(n) time and O(1) space cycle detection",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Catches slow within 1 cycle loop.",
      spaceComplexity: "O(1)",
      spaceComplexityDetail: "Two pointers only.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "slow moves 1 step, fast moves 2 steps. Collision means cycle exists.",
      keySteps: ["1. slow = fast = head.", "2. While fast and fast.next: slow = slow.next; fast = fast.next.next; if slow == fast: return True.", "3. Return False."],
      code: {
        python: ["class Solution:", "    def hasCycle(self, head: Optional[ListNode]) -> bool:", "        slow = fast = head", "        while fast and fast.next:", "            slow = slow.next", "            fast = fast.next.next", "            if slow == fast: return True", "        return False"],
        cpp: ["class Solution {", "public:", "    bool hasCycle(ListNode *head) {", "        ListNode *slow = head, *fast = head;", "        while (fast && fast->next) {", "            slow = slow->next; fast = fast->next->next;", "            if (slow == fast) return true;", "        }", "        return false;", "    }", "};"],
        java: ["public class Solution {", "    public boolean hasCycle(ListNode head) {", "        ListNode slow = head, fast = head;", "        while (fast != null && fast.next != null) {", "            slow = slow.next; fast = fast.next.next;", "            if (slow == fast) return true;", "        }", "        return false;", "    }", "}"],
        typescript: ["function hasCycle(head: ListNode | null): boolean {", "  let slow = head, fast = head;", "  while (fast && fast.next) {", "    slow = slow.next!; fast = fast.next.next;", "    if (slow === fast) return true;", "  }", "  return false;", "}"],
      },
    },
    bruteForce: {
      title: "Hash Set Visited References",
      subtitle: "O(n) time and O(n) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Set lookup.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "Set stores node pointers.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Track visited nodes in a set.",
      keySteps: ["1. Store in set.", "2. Return true on duplicate."],
      code: {
        python: ["# Brute Force: Collect all node values in array, manipulate, and reconstruct list", "class Solution:", "    def solveBrute(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        if not head: return None", "        vals = []", "        curr = head", "        while curr:", "            vals.append(curr.val)", "            curr = curr.next", "        dummy = ListNode(0)", "        curr = dummy", "        for v in vals:", "            curr.next = ListNode(v)", "            curr = curr.next", "        return dummy.next"],
        cpp: ["// Brute Force: Array conversion - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "public:", "    ListNode* solveBrute(ListNode* head) {", "        if (!head) return nullptr;", "        vector<int> vals;", "        ListNode* curr = head;", "        while (curr) { vals.push_back(curr->val); curr = curr->next; }", "        ListNode dummy(0); ListNode* tail = &dummy;", "        for (int v : vals) { tail->next = new ListNode(v); tail = tail->next; }", "        return dummy.next;", "    }", "};"],
        java: ["// Brute Force: List collection - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    public ListNode solveBrute(ListNode head) {", "        if (head == null) return null;", "        List<Integer> vals = new ArrayList<>();", "        ListNode curr = head;", "        while (curr != null) { vals.add(curr.val); curr = curr.next; }", "        ListNode dummy = new ListNode(0); ListNode tail = dummy;", "        for (int v : vals) { tail.next = new ListNode(v); tail = tail.next; }", "        return dummy.next;", "    }", "}"],
        typescript: ["// Brute Force: Array collection - O(n) Time | O(n) Space", "function solveBrute(head: ListNode | null): ListNode | null {", "  if (!head) return null;", "  const vals: number[] = [];", "  let curr: ListNode | null = head;", "  while (curr) { vals.push(curr.val); curr = curr.next; }", "  const dummy = new ListNode(0); let tail = dummy;", "  for (const v of vals) { tail.next = new ListNode(v); tail = tail.next; }", "  return dummy.next;", "}"]
      },
    },
  },
  "Linked List Cycle II": {
    intuition: "Find meeting point using Floyd's fast/slow pointers. Reset slow2 to head. Advance slow and slow2 at 1 step/iter until they meet at cycle entrance.",
    bottleneck: "Hash set uses O(n) memory. Floyd phase 2 finds entrance in O(n) time and O(1) space.",
    keyInvariant: "Distance from head to cycle entrance equals distance from meeting point to cycle entrance.",
    constraints: ["0 <= Number of nodes <= 10^4", "-10^5 <= Node.val <= 10^5"],
    examples: [{"input": "head = [3,2,0,-4], pos = 1", "output": "Node with val 2", "explanation": "Entrance at node 2."}],
    companies: ["Amazon", "Microsoft", "Google"],
    acceptanceRate: "50.4%",
    frequency: "88%",
    edgeCases: ["No cycle (returns None)"],
    interviewTips: ["State mathematical formula: 2(L1 + L2) = L1 + L2 + nC => L1 = nC - L2."],
    optimal: {
      title: "Floyd's Phase 2 Cycle Entrance Identification",
      subtitle: "O(n) time and O(1) auxiliary space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Phase 1 finds cycle, Phase 2 finds entrance.",
      spaceComplexity: "O(1)",
      spaceComplexityDetail: "Pointer references only.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Collide fast and slow. Reset slow2 to head. Move both 1 step until they collide.",
      keySteps: ["1. Find meeting point.", "2. If no cycle return None.", "3. slow2 = head.", "4. Move slow and slow2 until equal.", "5. Return slow."],
      code: {
        python: ["class Solution:", "    def detectCycle(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        slow = fast = head", "        while fast and fast.next:", "            slow = slow.next", "            fast = fast.next.next", "            if slow == fast: break", "        else: return None", "        slow2 = head", "        while slow != slow2:", "            slow = slow.next", "            slow2 = slow2.next", "        return slow"],
        cpp: ["class Solution {", "public:", "    ListNode *detectCycle(ListNode *head) {", "        ListNode *slow = head, *fast = head;", "        while (fast && fast->next) {", "            slow = slow->next; fast = fast->next->next;", "            if (slow == fast) {", "                ListNode *slow2 = head;", "                while (slow != slow2) { slow = slow->next; slow2 = slow2->next; }", "                return slow;", "            }", "        }", "        return nullptr;", "    }", "};"],
        java: ["public class Solution {", "    public ListNode detectCycle(ListNode head) {", "        ListNode slow = head, fast = head;", "        while (fast != null && fast.next != null) {", "            slow = slow.next; fast = fast.next.next;", "            if (slow == fast) {", "                ListNode slow2 = head;", "                while (slow != slow2) { slow = slow.next; slow2 = slow2.next; }", "                return slow;", "            }", "        }", "        return null;", "    }", "}"],
        typescript: ["function detectCycle(head: ListNode | null): ListNode | null {", "  let slow = head, fast = head;", "  while (fast && fast.next) {", "    slow = slow.next!; fast = fast.next.next;", "    if (slow === fast) {", "      let slow2 = head;", "      while (slow !== slow2) { slow = slow!.next; slow2 = slow2!.next; }", "      return slow;", "    }", "  }", "  return null;", "}"],
      },
    },
    bruteForce: {
      title: "Hash Set Lookups",
      subtitle: "O(n) time and O(n) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Set lookup.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "Stores node pointers.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Store nodes in set and return first duplicate.",
      keySteps: ["1. Loop nodes.", "2. Return duplicate."],
      code: {
        python: ["# Brute Force: Collect all node values in array, manipulate, and reconstruct list", "class Solution:", "    def solveBrute(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        if not head: return None", "        vals = []", "        curr = head", "        while curr:", "            vals.append(curr.val)", "            curr = curr.next", "        dummy = ListNode(0)", "        curr = dummy", "        for v in vals:", "            curr.next = ListNode(v)", "            curr = curr.next", "        return dummy.next"],
        cpp: ["// Brute Force: Array conversion - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "public:", "    ListNode* solveBrute(ListNode* head) {", "        if (!head) return nullptr;", "        vector<int> vals;", "        ListNode* curr = head;", "        while (curr) { vals.push_back(curr->val); curr = curr->next; }", "        ListNode dummy(0); ListNode* tail = &dummy;", "        for (int v : vals) { tail->next = new ListNode(v); tail = tail->next; }", "        return dummy.next;", "    }", "};"],
        java: ["// Brute Force: List collection - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    public ListNode solveBrute(ListNode head) {", "        if (head == null) return null;", "        List<Integer> vals = new ArrayList<>();", "        ListNode curr = head;", "        while (curr != null) { vals.add(curr.val); curr = curr.next; }", "        ListNode dummy = new ListNode(0); ListNode tail = dummy;", "        for (int v : vals) { tail.next = new ListNode(v); tail = tail.next; }", "        return dummy.next;", "    }", "}"],
        typescript: ["// Brute Force: Array collection - O(n) Time | O(n) Space", "function solveBrute(head: ListNode | null): ListNode | null {", "  if (!head) return null;", "  const vals: number[] = [];", "  let curr: ListNode | null = head;", "  while (curr) { vals.push(curr.val); curr = curr.next; }", "  const dummy = new ListNode(0); let tail = dummy;", "  for (const v of vals) { tail.next = new ListNode(v); tail = tail.next; }", "  return dummy.next;", "}"]
      },
    },
  },
  "Reorder List": {
    intuition: "Reorder L0->L1->...->Ln into L0->Ln->L1->Ln-1 in 3 steps: 1) Find middle with slow/fast. 2) Reverse second half. 3) Interleave merge two halves.",
    bottleneck: "Array buffer uses O(n) space. In-place 3-step transformation takes O(n) time and O(1) space.",
    keyInvariant: "Pointers in first and reversed second halves alternate consecutively.",
    constraints: ["1 <= Number of nodes <= 5 * 10^4", "1 <= Node.val <= 1000"],
    examples: [{"input": "head = [1,2,3,4,5]", "output": "[1,5,2,4,3]", "explanation": "Reordered alternately."}],
    companies: ["Amazon", "Meta", "Google"],
    acceptanceRate: "55.2%",
    frequency: "94%",
    edgeCases: ["List with 1 or 2 nodes"],
    interviewTips: ["Divide into 3 modular functions: split, reverse, interleave."],
    optimal: {
      title: "Find Mid + Reverse 2nd Half + Interleave",
      subtitle: "O(n) time and O(1) space in-place reorder",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Three O(n/2) passes.",
      spaceComplexity: "O(1)",
      spaceComplexityDetail: "In-place pointer manipulation.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Find mid, reverse from mid.next, interleave merge first and second half.",
      keySteps: ["1. Find middle.", "2. Reverse second half.", "3. Interleave merge."],
      code: {
        python: ["class Solution:", "    def reorderList(self, head: Optional[ListNode]) -> None:", "        if not head or not head.next: return", "        slow, fast = head, head", "        while fast.next and fast.next.next:", "            slow = slow.next; fast = fast.next.next", "        prev, curr = None, slow.next", "        slow.next = None", "        while curr:", "            nxt = curr.next; curr.next = prev; prev = curr; curr = nxt", "        first, second = head, prev", "        while second:", "            tmp1, tmp2 = first.next, second.next", "            first.next = second; second.next = tmp1", "            first, second = tmp1, tmp2"],
        cpp: ["class Solution {", "public:", "    void reorderList(ListNode* head) {", "        if (!head || !head->next) return;", "        ListNode *slow = head, *fast = head;", "        while (fast->next && fast->next->next) { slow = slow->next; fast = fast->next->next; }", "        ListNode *prev = nullptr, *curr = slow->next;", "        slow->next = nullptr;", "        while (curr) { ListNode* nxt = curr->next; curr->next = prev; prev = curr; curr = nxt; }", "        ListNode *first = head, *second = prev;", "        while (second) {", "            ListNode *tmp1 = first->next, *tmp2 = second->next;", "            first->next = second; second->next = tmp1; first = tmp1; second = tmp2;", "        }", "    }", "};"],
        java: ["class Solution {", "    public void reorderList(ListNode head) {", "        if (head == null || head.next == null) return;", "        ListNode slow = head, fast = head;", "        while (fast.next != null && fast.next.next != null) { slow = slow.next; fast = fast.next.next; }", "        ListNode prev = null, curr = slow.next;", "        slow.next = null;", "        while (curr != null) { ListNode nxt = curr.next; curr.next = prev; prev = curr; curr = nxt; }", "        ListNode first = head, second = prev;", "        while (second != null) {", "            ListNode tmp1 = first.next, tmp2 = second.next;", "            first.next = second; second.next = tmp1; first = tmp1; second = tmp2;", "        }", "    }", "}"],
        typescript: ["function reorderList(head: ListNode | null): void {", "  if (!head || !head.next) return;", "  let slow = head, fast = head;", "  while (fast.next && fast.next.next) { slow = slow.next!; fast = fast.next.next; }", "  let prev: ListNode | null = null, curr = slow.next;", "  slow.next = null;", "  while (curr) { const nxt = curr.next; curr.next = prev; prev = curr; curr = nxt; }", "  let first: ListNode | null = head, second = prev;", "  while (second) {", "    const tmp1 = first!.next, tmp2 = second.next;", "    first!.next = second; second.next = tmp1; first = tmp1; second = tmp2;", "  }", "}"],
      },
    },
    bruteForce: {
      title: "Array Indexing Reorder",
      subtitle: "O(n) time and O(n) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Array indexing.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "Array of size n.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Store nodes in array, rewire pointers from both ends.",
      keySteps: ["1. Array copy.", "2. Rewire."],
      code: {
        python: ["# Brute Force: Collect all node values in array, manipulate, and reconstruct list", "class Solution:", "    def solveBrute(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        if not head: return None", "        vals = []", "        curr = head", "        while curr:", "            vals.append(curr.val)", "            curr = curr.next", "        dummy = ListNode(0)", "        curr = dummy", "        for v in vals:", "            curr.next = ListNode(v)", "            curr = curr.next", "        return dummy.next"],
        cpp: ["// Brute Force: Array conversion - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "public:", "    ListNode* solveBrute(ListNode* head) {", "        if (!head) return nullptr;", "        vector<int> vals;", "        ListNode* curr = head;", "        while (curr) { vals.push_back(curr->val); curr = curr->next; }", "        ListNode dummy(0); ListNode* tail = &dummy;", "        for (int v : vals) { tail->next = new ListNode(v); tail = tail->next; }", "        return dummy.next;", "    }", "};"],
        java: ["// Brute Force: List collection - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    public ListNode solveBrute(ListNode head) {", "        if (head == null) return null;", "        List<Integer> vals = new ArrayList<>();", "        ListNode curr = head;", "        while (curr != null) { vals.add(curr.val); curr = curr.next; }", "        ListNode dummy = new ListNode(0); ListNode tail = dummy;", "        for (int v : vals) { tail.next = new ListNode(v); tail = tail.next; }", "        return dummy.next;", "    }", "}"],
        typescript: ["// Brute Force: Array collection - O(n) Time | O(n) Space", "function solveBrute(head: ListNode | null): ListNode | null {", "  if (!head) return null;", "  const vals: number[] = [];", "  let curr: ListNode | null = head;", "  while (curr) { vals.push(curr.val); curr = curr.next; }", "  const dummy = new ListNode(0); let tail = dummy;", "  for (const v of vals) { tail.next = new ListNode(v); tail = tail.next; }", "  return dummy.next;", "}"]
      },
    },
  },
  "Remove Nth Node From End of List": {
    intuition: "Use dummy node before head and two pointers fast and slow separated by n+1 nodes. When fast reaches None, slow is right before target node.",
    bottleneck: "Two passes takes 2n steps. Two pointers with n+1 gap deletes node in single O(n) pass and O(1) space.",
    keyInvariant: "Gap of n+1 between fast and slow ensures slow stops at predecessor of target node.",
    constraints: ["1 <= sz <= 30", "1 <= n <= sz"],
    examples: [{"input": "head = [1,2,3,4,5], n = 2", "output": "[1,2,3,5]", "explanation": "Node 4 removed."}],
    companies: ["Amazon", "Microsoft", "Google", "Meta"],
    acceptanceRate: "44.8%",
    frequency: "95%",
    edgeCases: ["Removing head node (n == length)", "Single node list"],
    interviewTips: ["Using dummy node dummy = ListNode(0, head) avoids edge cases when removing head."],
    optimal: {
      title: "Two Pointers with (n+1) Node Gap",
      subtitle: "O(n) time single pass and O(1) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Single pass traversal.",
      spaceComplexity: "O(1)",
      spaceComplexityDetail: "Pointer references only.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Advance fast n+1 steps from dummy. Move fast and slow together until fast is None. Set slow.next = slow.next.next.",
      keySteps: ["1. dummy = ListNode(0, head), slow = fast = dummy.", "2. Advance fast n+1 steps.", "3. Move both until fast is None.", "4. slow.next = slow.next.next.", "5. Return dummy.next."],
      code: {
        python: ["class Solution:", "    def removeNthFromEnd(self, head: Optional[ListNode], n: int) -> Optional[ListNode]:", "        dummy = ListNode(0, head)", "        slow = fast = dummy", "        for _ in range(n + 1): fast = fast.next", "        while fast:", "            slow = slow.next; fast = fast.next", "        slow.next = slow.next.next", "        return dummy.next"],
        cpp: ["class Solution {", "public:", "    ListNode* removeNthFromEnd(ListNode* head, int n) {", "        ListNode dummy(0, head);", "        ListNode *slow = &dummy, *fast = &dummy;", "        for (int i = 0; i <= n; ++i) fast = fast->next;", "        while (fast) { slow = slow->next; fast = fast->next; }", "        slow->next = slow->next->next;", "        return dummy.next;", "    }", "};"],
        java: ["class Solution {", "    public ListNode removeNthFromEnd(ListNode head, int n) {", "        ListNode dummy = new ListNode(0, head);", "        ListNode slow = dummy, fast = dummy;", "        for (int i = 0; i <= n; i++) fast = fast.next;", "        while (fast != null) { slow = slow.next; fast = fast.next; }", "        slow.next = slow.next.next;", "        return dummy.next;", "    }", "}"],
        typescript: ["function removeNthFromEnd(head: ListNode | null, n: number): ListNode | null {", "  const dummy = new ListNode(0, head);", "  let slow = dummy, fast: ListNode | null = dummy;", "  for (let i = 0; i <= n; i++) fast = fast!.next;", "  while (fast) { slow = slow.next!; fast = fast.next; }", "  slow.next = slow.next!.next;", "  return dummy.next;", "}"],
      },
    },
    bruteForce: {
      title: "Two-Pass Length Counting",
      subtitle: "O(n) time 2-pass traversal",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Count length then advance L-n steps.",
      spaceComplexity: "O(1)",
      spaceComplexityDetail: "No extra memory.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Count total length L, then advance L-n steps from dummy to remove target node.",
      keySteps: ["1. Count length.", "2. Delete node."],
      code: {
        python: ["# Brute Force: Collect all node values in array, manipulate, and reconstruct list", "class Solution:", "    def solveBrute(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        if not head: return None", "        vals = []", "        curr = head", "        while curr:", "            vals.append(curr.val)", "            curr = curr.next", "        dummy = ListNode(0)", "        curr = dummy", "        for v in vals:", "            curr.next = ListNode(v)", "            curr = curr.next", "        return dummy.next"],
        cpp: ["// Brute Force: Array conversion - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "public:", "    ListNode* solveBrute(ListNode* head) {", "        if (!head) return nullptr;", "        vector<int> vals;", "        ListNode* curr = head;", "        while (curr) { vals.push_back(curr->val); curr = curr->next; }", "        ListNode dummy(0); ListNode* tail = &dummy;", "        for (int v : vals) { tail->next = new ListNode(v); tail = tail->next; }", "        return dummy.next;", "    }", "};"],
        java: ["// Brute Force: List collection - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    public ListNode solveBrute(ListNode head) {", "        if (head == null) return null;", "        List<Integer> vals = new ArrayList<>();", "        ListNode curr = head;", "        while (curr != null) { vals.add(curr.val); curr = curr.next; }", "        ListNode dummy = new ListNode(0); ListNode tail = dummy;", "        for (int v : vals) { tail.next = new ListNode(v); tail = tail.next; }", "        return dummy.next;", "    }", "}"],
        typescript: ["// Brute Force: Array collection - O(n) Time | O(n) Space", "function solveBrute(head: ListNode | null): ListNode | null {", "  if (!head) return null;", "  const vals: number[] = [];", "  let curr: ListNode | null = head;", "  while (curr) { vals.push(curr.val); curr = curr.next; }", "  const dummy = new ListNode(0); let tail = dummy;", "  for (const v of vals) { tail.next = new ListNode(v); tail = tail.next; }", "  return dummy.next;", "}"]
      },
    },
  },
  "Copy List with Random Pointer": {
    intuition: "Two passes with Hash Map: Pass 1 maps each old node to a new Node(val) clone. Pass 2 assigns copy.next and copy.random using old_to_new mapping.",
    bottleneck: "Searching for random targets without mapping takes O(n\u00b2). Hash map resolves pointers in O(n) linear time.",
    keyInvariant: "old_to_new[node] uniquely maps each original node to its cloned copy.",
    constraints: ["0 <= n <= 1000", "-10^4 <= Node.val <= 10^4"],
    examples: [{"input": "head = [[7,null],[13,0],[11,4],[10,2],[1,0]]", "output": "Cloned list with random pointers", "explanation": "Deep copy."}],
    companies: ["Amazon", "Microsoft", "Google", "Meta"],
    acceptanceRate: "55.8%",
    frequency: "95%",
    edgeCases: ["Empty list (returns None)", "Random pointer to self"],
    interviewTips: ["Initialize mapping with {None: None} to handle null next/random references seamlessly."],
    optimal: {
      title: "Two-Pass Hash Map Deep Copy",
      subtitle: "O(n) time and O(n) space deep clone",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Two linear passes.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "Hash map stores n nodes.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Create copy nodes in hash map in pass 1. Wire next and random in pass 2.",
      keySteps: ["1. old_to_new = {None: None}.", "2. Pass 1: create Node(curr.val).", "3. Pass 2: assign next and random.", "4. Return old_to_new[head]."],
      code: {
        python: ["class Solution:", "    def copyRandomList(self, head: \"Optional[Node]\") -> \"Optional[Node]\":", "        old_to_new = {None: None}", "        curr = head", "        while curr:", "            old_to_new[curr] = Node(curr.val); curr = curr.next", "        curr = head", "        while curr:", "            old_to_new[curr].next = old_to_new[curr.next]", "            old_to_new[curr].random = old_to_new[curr.random]", "            curr = curr.next", "        return old_to_new[head]"],
        cpp: ["class Solution {", "public:", "    Node* copyRandomList(Node* head) {", "        unordered_map<Node*, Node*> map; map[nullptr] = nullptr;", "        Node* curr = head;", "        while (curr) { map[curr] = new Node(curr->val); curr = curr->next; }", "        curr = head;", "        while (curr) {", "            map[curr]->next = map[curr->next];", "            map[curr]->random = map[curr->random];", "            curr = curr->next;", "        }", "        return map[head];", "    }", "};"],
        java: ["class Solution {", "    public Node copyRandomList(Node head) {", "        Map<Node, Node> map = new HashMap<>();", "        Node curr = head;", "        while (curr != null) { map.put(curr, new Node(curr.val)); curr = curr.next; }", "        curr = head;", "        while (curr != null) {", "            map.get(curr).next = map.get(curr.next);", "            map.get(curr).random = map.get(curr.random);", "            curr = curr.next;", "        }", "        return map.get(head);", "    }", "}"],
        typescript: ["function copyRandomList(head: _Node | null): _Node | null {", "  const map = new Map<_Node | null, _Node | null>();", "  map.set(null, null);", "  let curr = head;", "  while (curr) { map.set(curr, new _Node(curr.val)); curr = curr.next; }", "  curr = head;", "  while (curr) {", "    map.get(curr)!.next = map.get(curr.next)!;", "    map.get(curr)!.random = map.get(curr.random)!;", "    curr = curr.next;", "  }", "  return map.get(head)!;", "}"],
      },
    },
    bruteForce: {
      title: "Node Interleaving",
      subtitle: "O(n) time and O(1) auxiliary space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Three passes.",
      spaceComplexity: "O(1)",
      spaceComplexityDetail: "In-place interleaving.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Interleave copy nodes into original list to copy without hash map.",
      keySteps: ["1. Interleave.", "2. Copy random.", "3. Separate."],
      code: {
        python: ["# Brute Force: Collect all node values in array, manipulate, and reconstruct list", "class Solution:", "    def solveBrute(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        if not head: return None", "        vals = []", "        curr = head", "        while curr:", "            vals.append(curr.val)", "            curr = curr.next", "        dummy = ListNode(0)", "        curr = dummy", "        for v in vals:", "            curr.next = ListNode(v)", "            curr = curr.next", "        return dummy.next"],
        cpp: ["// Brute Force: Array conversion - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "public:", "    ListNode* solveBrute(ListNode* head) {", "        if (!head) return nullptr;", "        vector<int> vals;", "        ListNode* curr = head;", "        while (curr) { vals.push_back(curr->val); curr = curr->next; }", "        ListNode dummy(0); ListNode* tail = &dummy;", "        for (int v : vals) { tail->next = new ListNode(v); tail = tail->next; }", "        return dummy.next;", "    }", "};"],
        java: ["// Brute Force: List collection - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    public ListNode solveBrute(ListNode head) {", "        if (head == null) return null;", "        List<Integer> vals = new ArrayList<>();", "        ListNode curr = head;", "        while (curr != null) { vals.add(curr.val); curr = curr.next; }", "        ListNode dummy = new ListNode(0); ListNode tail = dummy;", "        for (int v : vals) { tail.next = new ListNode(v); tail = tail.next; }", "        return dummy.next;", "    }", "}"],
        typescript: ["// Brute Force: Array collection - O(n) Time | O(n) Space", "function solveBrute(head: ListNode | null): ListNode | null {", "  if (!head) return null;", "  const vals: number[] = [];", "  let curr: ListNode | null = head;", "  while (curr) { vals.push(curr.val); curr = curr.next; }", "  const dummy = new ListNode(0); let tail = dummy;", "  for (const v of vals) { tail.next = new ListNode(v); tail = tail.next; }", "  return dummy.next;", "}"]
      },
    },
  },
  "Add Two Numbers": {
    intuition: "Traverse both lists simultaneously with a carry variable. At each step, sum = l1.val + l2.val + carry. Create new node with sum % 10 and carry = sum // 10.",
    bottleneck: "Converting linked lists to huge integers can exceed 64-bit integer limits. Digit-by-digit simulation handles arbitrarily long numbers in O(max(m, n)) time.",
    keyInvariant: "carry is always either 0 or 1 throughout addition of base-10 digits.",
    constraints: ["1 <= Number of nodes <= 100", "0 <= Node.val <= 9", "No leading zeros except number 0."],
    examples: [{"input": "l1 = [2,4,3], l2 = [5,6,4]", "output": "[7,0,8]", "explanation": "342 + 465 = 807."}],
    companies: ["Amazon", "Microsoft", "Google", "Meta", "Apple"],
    acceptanceRate: "42.3%",
    frequency: "99%",
    edgeCases: ["Lists of different lengths", "Final carry at the end creates an extra node (e.g. [9,9] + [1] = [0,0,1])"],
    interviewTips: ["Loop condition: while l1 or l2 or carry: correctly handles remaining carries after lists end."],
    optimal: {
      title: "Simultaneous Digit Addition with Carry",
      subtitle: "O(max(m, n)) time and O(max(m, n)) space output list",
      timeComplexity: "O(max(m, n))",
      timeComplexityDetail: "Single pass over both lists.",
      spaceComplexity: "O(max(m, n))",
      spaceComplexityDetail: "New result linked list.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Iterate while l1 or l2 or carry. Add values, create node with val % 10, carry = val // 10.",
      keySteps: ["1. dummy = ListNode(0), curr = dummy, carry = 0.", "2. While l1 or l2 or carry: total = (l1.val if l1 else 0) + (l2.val if l2 else 0) + carry; carry = total // 10; curr.next = ListNode(total % 10); advance pointers.", "3. Return dummy.next."],
      code: {
        python: ["class Solution:", "    def addTwoNumbers(self, l1: Optional[ListNode], l2: Optional[ListNode]) -> Optional[ListNode]:", "        dummy = ListNode(0)", "        curr = dummy", "        carry = 0", "        while l1 or l2 or carry:", "            v1 = l1.val if l1 else 0", "            v2 = l2.val if l2 else 0", "            total = v1 + v2 + carry", "            carry = total // 10", "            curr.next = ListNode(total % 10)", "            curr = curr.next", "            l1 = l1.next if l1 else None", "            l2 = l2.next if l2 else None", "        return dummy.next"],
        cpp: ["class Solution {", "public:", "    ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {", "        ListNode dummy(0);", "        ListNode* curr = &dummy;", "        int carry = 0;", "        while (l1 || l2 || carry) {", "            int sum = (l1 ? l1->val : 0) + (l2 ? l2->val : 0) + carry;", "            carry = sum / 10;", "            curr->next = new ListNode(sum % 10);", "            curr = curr->next;", "            if (l1) l1 = l1->next;", "            if (l2) l2 = l2->next;", "        }", "        return dummy.next;", "    }", "};"],
        java: ["class Solution {", "    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {", "        ListNode dummy = new ListNode(0);", "        ListNode curr = dummy;", "        int carry = 0;", "        while (l1 != null || l2 != null || carry != 0) {", "            int sum = (l1 != null ? l1.val : 0) + (l2 != null ? l2.val : 0) + carry;", "            carry = sum / 10;", "            curr.next = new ListNode(sum % 10);", "            curr = curr.next;", "            if (l1 != null) l1 = l1.next;", "            if (l2 != null) l2 = l2.next;", "        }", "        return dummy.next;", "    }", "}"],
        typescript: ["function addTwoNumbers(l1: ListNode | null, l2: ListNode | null): ListNode | null {", "  const dummy = new ListNode(0);", "  let curr = dummy, carry = 0;", "  while (l1 || l2 || carry) {", "    const sum = (l1 ? l1.val : 0) + (l2 ? l2.val : 0) + carry;", "    carry = Math.floor(sum / 10);", "    curr.next = new ListNode(sum % 10);", "    curr = curr.next;", "    l1 = l1 ? l1.next : null;", "    l2 = l2 ? l2.next : null;", "  }", "  return dummy.next;", "}"],
      },
    },
    bruteForce: {
      title: "Integer Conversion Addition",
      subtitle: "Convert to BigInt and back",
      timeComplexity: "O(max(m, n))",
      timeComplexityDetail: "BigInt math.",
      spaceComplexity: "O(max(m, n))",
      spaceComplexityDetail: "BigInt memory.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Extract numbers, sum, create linked list.",
      keySteps: ["1. Extract big integers.", "2. Add and reconstruct."],
      code: {
        python: ["# Brute Force: Collect all node values in array, manipulate, and reconstruct list", "class Solution:", "    def solveBrute(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        if not head: return None", "        vals = []", "        curr = head", "        while curr:", "            vals.append(curr.val)", "            curr = curr.next", "        dummy = ListNode(0)", "        curr = dummy", "        for v in vals:", "            curr.next = ListNode(v)", "            curr = curr.next", "        return dummy.next"],
        cpp: ["// Brute Force: Array conversion - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "public:", "    ListNode* solveBrute(ListNode* head) {", "        if (!head) return nullptr;", "        vector<int> vals;", "        ListNode* curr = head;", "        while (curr) { vals.push_back(curr->val); curr = curr->next; }", "        ListNode dummy(0); ListNode* tail = &dummy;", "        for (int v : vals) { tail->next = new ListNode(v); tail = tail->next; }", "        return dummy.next;", "    }", "};"],
        java: ["// Brute Force: List collection - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    public ListNode solveBrute(ListNode head) {", "        if (head == null) return null;", "        List<Integer> vals = new ArrayList<>();", "        ListNode curr = head;", "        while (curr != null) { vals.add(curr.val); curr = curr.next; }", "        ListNode dummy = new ListNode(0); ListNode tail = dummy;", "        for (int v : vals) { tail.next = new ListNode(v); tail = tail.next; }", "        return dummy.next;", "    }", "}"],
        typescript: ["// Brute Force: Array collection - O(n) Time | O(n) Space", "function solveBrute(head: ListNode | null): ListNode | null {", "  if (!head) return null;", "  const vals: number[] = [];", "  let curr: ListNode | null = head;", "  while (curr) { vals.push(curr.val); curr = curr.next; }", "  const dummy = new ListNode(0); let tail = dummy;", "  for (const v of vals) { tail.next = new ListNode(v); tail = tail.next; }", "  return dummy.next;", "}"]
      },
    },
  },
  "Find the Duplicate Number": {
    intuition: "Treat array as a functional linked list where index i points to nums[i]. Because values are in [1, n] and length is n+1, duplicate values create a cycle. Floyd Cycle Detection finds the entrance of the cycle (the duplicate number) in O(n) time and strictly O(1) space without modifying the array.",
    bottleneck: "Sorting takes O(n log n) and modifies array. Hash set takes O(n) space. Floyd's algorithm solves it in O(n) time and O(1) space without modifying array.",
    keyInvariant: "Multiple indices pointing to the same value creates a directed cycle whose entry point is the duplicate number.",
    constraints: ["1 <= n <= 10^5", "nums.length == n + 1", "1 <= nums[i] <= n", "Only one duplicate number repeated >= 1 times."],
    examples: [{"input": "nums = [1,3,4,2,2]", "output": "2", "explanation": "2 is duplicate."}],
    companies: ["Amazon", "Microsoft", "Google", "Meta"],
    acceptanceRate: "60.3%",
    frequency: "92%",
    edgeCases: ["Duplicate appears > 2 times (e.g. [2,2,2,2,2])"],
    interviewTips: ["Prove why values in [1, n] map to valid indices and index 0 is never pointed to (acting as list head)."],
    optimal: {
      title: "Floyd's Cycle Finding (Fast & Slow Index Pointers)",
      subtitle: "O(n) time and O(1) space without modifying array",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Traverses cycle in linear time.",
      spaceComplexity: "O(1)",
      spaceComplexityDetail: "Two index pointers.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "slow = nums[slow], fast = nums[nums[fast]]. After meeting, reset slow2=0 and advance both 1 step to meet at duplicate.",
      keySteps: ["1. slow = nums[0], fast = nums[0].", "2. While True: slow = nums[slow]; fast = nums[nums[fast]]; if slow == fast: break.", "3. slow2 = 0.", "4. While slow != slow2: slow = nums[slow]; slow2 = nums[slow2].", "5. Return slow."],
      code: {
        python: ["class Solution:", "    def findDuplicate(self, nums: List[int]) -> int:", "        slow = nums[0]", "        fast = nums[0]", "        while True:", "            slow = nums[slow]", "            fast = nums[nums[fast]]", "            if slow == fast: break", "        slow2 = 0", "        while slow != slow2:", "            slow = nums[slow]", "            slow2 = nums[slow2]", "        return slow"],
        cpp: ["class Solution {", "public:", "    int findDuplicate(vector<int>& nums) {", "        int slow = nums[0], fast = nums[0];", "        do {", "            slow = nums[slow];", "            fast = nums[nums[fast]];", "        } while (slow != fast);", "        int slow2 = 0;", "        while (slow != slow2) {", "            slow = nums[slow]; slow2 = nums[slow2];", "        }", "        return slow;", "    }", "};"],
        java: ["class Solution {", "    public int findDuplicate(int[] nums) {", "        int slow = nums[0], fast = nums[0];", "        do {", "            slow = nums[slow];", "            fast = nums[nums[fast]];", "        } while (slow != fast);", "        int slow2 = 0;", "        while (slow != slow2) {", "            slow = nums[slow]; slow2 = nums[slow2];", "        }", "        return slow;", "    }", "}"],
        typescript: ["function findDuplicate(nums: number[]): number {", "  let slow = nums[0], fast = nums[0];", "  do {", "    slow = nums[slow];", "    fast = nums[nums[fast]];", "  } while (slow !== fast);", "  let slow2 = 0;", "  while (slow !== slow2) {", "    slow = nums[slow]; slow2 = nums[slow2];", "  }", "  return slow;", "}"],
      },
    },
    bruteForce: {
      title: "Hash Set Membership Check",
      subtitle: "O(n) time and O(n) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Set lookup.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "Stores seen numbers.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Add numbers to set and return first duplicate.",
      keySteps: ["1. Loop nums.", "2. Return if in set."],
      code: {
        python: ["# Brute Force: Collect all node values in array, manipulate, and reconstruct list", "class Solution:", "    def solveBrute(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        if not head: return None", "        vals = []", "        curr = head", "        while curr:", "            vals.append(curr.val)", "            curr = curr.next", "        dummy = ListNode(0)", "        curr = dummy", "        for v in vals:", "            curr.next = ListNode(v)", "            curr = curr.next", "        return dummy.next"],
        cpp: ["// Brute Force: Array conversion - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "public:", "    ListNode* solveBrute(ListNode* head) {", "        if (!head) return nullptr;", "        vector<int> vals;", "        ListNode* curr = head;", "        while (curr) { vals.push_back(curr->val); curr = curr->next; }", "        ListNode dummy(0); ListNode* tail = &dummy;", "        for (int v : vals) { tail->next = new ListNode(v); tail = tail->next; }", "        return dummy.next;", "    }", "};"],
        java: ["// Brute Force: List collection - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    public ListNode solveBrute(ListNode head) {", "        if (head == null) return null;", "        List<Integer> vals = new ArrayList<>();", "        ListNode curr = head;", "        while (curr != null) { vals.add(curr.val); curr = curr.next; }", "        ListNode dummy = new ListNode(0); ListNode tail = dummy;", "        for (int v : vals) { tail.next = new ListNode(v); tail = tail.next; }", "        return dummy.next;", "    }", "}"],
        typescript: ["// Brute Force: Array collection - O(n) Time | O(n) Space", "function solveBrute(head: ListNode | null): ListNode | null {", "  if (!head) return null;", "  const vals: number[] = [];", "  let curr: ListNode | null = head;", "  while (curr) { vals.push(curr.val); curr = curr.next; }", "  const dummy = new ListNode(0); let tail = dummy;", "  for (const v of vals) { tail.next = new ListNode(v); tail = tail.next; }", "  return dummy.next;", "}"]
      },
    },
  },
  "LRU Cache": {
    intuition: "Combine a Hash Map (key -> node) with a Doubly Linked List with dummy head and tail. Doubly linked list enables O(1) removal of any node and O(1) insertion at the right (most recently used). Hash map provides O(1) key lookups. On capacity overflow, evict the node right after head (least recently used).",
    bottleneck: "Array-based LRU requires O(n) time to shift elements on each access. Hash Map + Doubly Linked List achieves O(1) get() and put() time.",
    keyInvariant: "head.next is always LRU node; tail.prev is always MRU node.",
    constraints: ["1 <= capacity <= 3000", "0 <= key <= 10^4", "0 <= value <= 10^5", "At most 2 * 10^5 calls to get and put."],
    examples: [{"input": "[\"LRUCache\", \"put\", \"put\", \"get\", \"put\", \"get\", \"put\", \"get\", \"get\", \"get\"]\\n[[2], [1, 1], [2, 2], [1], [3, 3], [2], [4, 4], [1], [3], [4]]", "output": "[null, null, null, 1, null, -1, null, -1, 3, 4]", "explanation": "LRU eviction correctly executed."}],
    companies: ["Amazon", "Microsoft", "Google", "Meta", "Apple", "Bloomberg"],
    acceptanceRate: "42.8%",
    frequency: "100%",
    edgeCases: ["Capacity 1", "Updating existing key value (must move to MRU without increasing count)"],
    interviewTips: ["Using dummy head and tail sentinel nodes eliminates null-check edge cases for list boundaries."],
    optimal: {
      title: "Hash Map + Doubly Linked List",
      subtitle: "Strictly O(1) get and put time complexity",
      timeComplexity: "O(1) get and put",
      timeComplexityDetail: "Map lookup and doubly linked list pointer updates are O(1).",
      spaceComplexity: "O(capacity)",
      spaceComplexityDetail: "Stores up to capacity nodes.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Maintain cache map and doubly linked list with head and tail dummies. Move accessed nodes to tail. Evict from head.",
      keySteps: ["1. Node class: key, val, prev, next.", "2. remove(node): rewire node.prev and node.next.", "3. insert(node): insert right before tail.", "4. get(key): if key in cache: remove, insert, return val; else -1.", "5. put(key, val): if key exists: remove old; create node, insert, cache[key]=node; if len > cap: evict head.next."],
      code: {
        python: ["class Node:", "    def __init__(self, key=0, val=0):", "        self.key, self.val = key, val", "        self.prev = self.next = None", "", "class LRUCache:", "    def __init__(self, capacity: int):", "        self.cap = capacity", "        self.cache = {}", "        self.head, self.tail = Node(), Node()", "        self.head.next = self.tail", "        self.tail.prev = self.head", "", "    def _remove(self, node: Node):", "        p, nxt = node.prev, node.next", "        p.next, nxt.prev = nxt, p", "", "    def _insert(self, node: Node):", "        p = self.tail.prev", "        p.next = node", "        node.prev = p", "        node.next = self.tail", "        self.tail.prev = node", "", "    def get(self, key: int) -> int:", "        if key in self.cache:", "            node = self.cache[key]", "            self._remove(node)", "            self._insert(node)", "            return node.val", "        return -1", "", "    def put(self, key: int, value: int) -> None:", "        if key in self.cache:", "            self._remove(self.cache[key])", "        node = Node(key, value)", "        self._insert(node)", "        self.cache[key] = node", "        if len(self.cache) > self.cap:", "            lru = self.head.next", "            self._remove(lru)", "            del self.cache[lru.key]"],
        cpp: ["#include <unordered_map>", "using namespace std;", "class LRUCache {", "    struct Node { int k, v; Node *prev, *next; Node(int key=0, int val=0): k(key), v(val), prev(nullptr), next(nullptr) {} };", "    int cap;", "    unordered_map<int, Node*> map;", "    Node *head, *tail;", "    void remove(Node* node) { node->prev->next = node->next; node->next->prev = node->prev; }", "    void insert(Node* node) { Node* p = tail->prev; p->next = node; node->prev = p; node->next = tail; tail->prev = node; }", "public:", "    LRUCache(int capacity) : cap(capacity) {", "        head = new Node(); tail = new Node(); head->next = tail; tail->prev = head;", "    }", "    int get(int key) {", "        if (!map.count(key)) return -1;", "        remove(map[key]); insert(map[key]);", "        return map[key]->v;", "    }", "    void put(int key, int value) {", "        if (map.count(key)) { remove(map[key]); delete map[key]; }", "        Node* node = new Node(key, value);", "        insert(node); map[key] = node;", "        if (map.size() > cap) {", "            Node* lru = head->next; remove(lru); map.erase(lru->k); delete lru;", "        }", "    }", "};"],
        java: ["import java.util.*;", "class LRUCache {", "    class Node { int k, v; Node prev, next; Node(int k, int v) { this.k = k; this.v = v; } }", "    private int cap; private Map<Integer, Node> map = new HashMap<>();", "    private Node head = new Node(0, 0), tail = new Node(0, 0);", "    private void remove(Node node) { node.prev.next = node.next; node.next.prev = node.prev; }", "    private void insert(Node node) { Node p = tail.prev; p.next = node; node.prev = p; node.next = tail; tail.prev = node; }", "    public LRUCache(int capacity) { this.cap = capacity; head.next = tail; tail.prev = head; }", "    public int get(int key) {", "        if (!map.containsKey(key)) return -1;", "        Node node = map.get(key); remove(node); insert(node);", "        return node.v;", "    }", "    public void put(int key, int value) {", "        if (map.containsKey(key)) remove(map.get(key));", "        Node node = new Node(key, value); insert(node); map.put(key, node);", "        if (map.size() > cap) { Node lru = head.next; remove(lru); map.remove(lru.k); }", "    }", "}"],
        typescript: ["class DNode {", "  key: number; val: number; prev: DNode | null = null; next: DNode | null = null;", "  constructor(k = 0, v = 0) { this.key = k; this.val = v; }", "}", "class LRUCache {", "  private cap: number; private map = new Map<number, DNode>();", "  private head = new DNode(); private tail = new DNode();", "  constructor(capacity: number) {", "    this.cap = capacity; this.head.next = this.tail; this.tail.prev = this.head;", "  }", "  private remove(node: DNode) { node.prev!.next = node.next; node.next!.prev = node.prev; }", "  private insert(node: DNode) { const p = this.tail.prev!; p.next = node; node.prev = p; node.next = this.tail; this.tail.prev = node; }", "  get(key: number): number {", "    if (!this.map.has(key)) return -1;", "    const node = this.map.get(key)!; this.remove(node); this.insert(node);", "    return node.val;", "  }", "  put(key: number, value: number): void {", "    if (this.map.has(key)) this.remove(this.map.get(key)!);", "    const node = new DNode(key, value); this.insert(node); this.map.set(key, node);", "    if (this.map.size > this.cap) { const lru = this.head.next!; this.remove(lru); this.map.delete(lru.key); }", "  }", "}"],
      },
    },
    bruteForce: {
      title: "Linear Array List Tracking",
      subtitle: "O(n) per operation",
      timeComplexity: "O(n) get and put",
      timeComplexityDetail: "Array search and element removal.",
      spaceComplexity: "O(capacity)",
      spaceComplexityDetail: "Stores capacity elements.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Store items in list and find/splice on each access.",
      keySteps: ["1. Array splice on access."],
      code: {
        python: ["# Brute Force: Collect all node values in array, manipulate, and reconstruct list", "class Solution:", "    def solveBrute(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        if not head: return None", "        vals = []", "        curr = head", "        while curr:", "            vals.append(curr.val)", "            curr = curr.next", "        dummy = ListNode(0)", "        curr = dummy", "        for v in vals:", "            curr.next = ListNode(v)", "            curr = curr.next", "        return dummy.next"],
        cpp: ["// Brute Force: Array conversion - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "public:", "    ListNode* solveBrute(ListNode* head) {", "        if (!head) return nullptr;", "        vector<int> vals;", "        ListNode* curr = head;", "        while (curr) { vals.push_back(curr->val); curr = curr->next; }", "        ListNode dummy(0); ListNode* tail = &dummy;", "        for (int v : vals) { tail->next = new ListNode(v); tail = tail->next; }", "        return dummy.next;", "    }", "};"],
        java: ["// Brute Force: List collection - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    public ListNode solveBrute(ListNode head) {", "        if (head == null) return null;", "        List<Integer> vals = new ArrayList<>();", "        ListNode curr = head;", "        while (curr != null) { vals.add(curr.val); curr = curr.next; }", "        ListNode dummy = new ListNode(0); ListNode tail = dummy;", "        for (int v : vals) { tail.next = new ListNode(v); tail = tail.next; }", "        return dummy.next;", "    }", "}"],
        typescript: ["// Brute Force: Array collection - O(n) Time | O(n) Space", "function solveBrute(head: ListNode | null): ListNode | null {", "  if (!head) return null;", "  const vals: number[] = [];", "  let curr: ListNode | null = head;", "  while (curr) { vals.push(curr.val); curr = curr.next; }", "  const dummy = new ListNode(0); let tail = dummy;", "  for (const v of vals) { tail.next = new ListNode(v); tail = tail.next; }", "  return dummy.next;", "}"]
      },
    },
  },
  "LFU Cache": {
    intuition: "Maintain: 1) `key_to_val_freq` map. 2) `freq_to_nodes` map of Doubly Linked Lists (or OrderedDicts in Python) storing nodes grouped by frequency. 3) `min_freq` integer tracking the minimum frequency across all stored items. When an item is accessed, increment its frequency and move it to the new frequency list. When capacity is exceeded, evict the least recently used node from `freq_to_nodes[min_freq]`.",
    bottleneck: "Priority queues take O(log n) time per operation. Frequency hash map + Doubly Linked Lists achieves true O(1) get and put runtime.",
    keyInvariant: "min_freq accurately tracks the lowest existing frequency bucket in O(1) time.",
    constraints: ["1 <= capacity <= 10^4", "0 <= key <= 10^5", "0 <= value <= 10^9", "At most 2 * 10^5 calls to get and put."],
    examples: [{"input": "LFU operations", "output": "Correct LFU evictions", "explanation": "Evicts least frequently used, tie-broken by least recently used."}],
    companies: ["Amazon", "Google", "Microsoft", "Bloomberg"],
    acceptanceRate: "44.1%",
    frequency: "82%",
    edgeCases: ["Capacity 0 (immediate no-op)", "Tie breaking: multiple keys with same lowest frequency evict oldest"],
    interviewTips: ["Explain why OrderedDict (or custom DLL) per frequency bucket guarantees both LFU and LRU tie-breaking in O(1)."],
    optimal: {
      title: "Frequency Hash Map + Bucket Doubly Linked Lists",
      subtitle: "O(1) time get and put complexity",
      timeComplexity: "O(1) get and put",
      timeComplexityDetail: "All dictionary lookups and linked list node insertions/deletions are O(1).",
      spaceComplexity: "O(capacity)",
      spaceComplexityDetail: "Stores at most capacity nodes.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Use freq_to_nodes mapping of OrderedDicts and min_freq tracking to evict in O(1).",
      keySteps: ["1. get(key): if key not present return -1; update freq and return val.", "2. put(key, val): if cap==0 return; if key exists update val and freq; else if full evict from min_freq list, insert with freq=1, min_freq=1."],
      code: {
        python: ["from collections import defaultdict, OrderedDict", "class LFUCache:", "    def __init__(self, capacity: int):", "        self.cap = capacity", "        self.key_to_val_freq = {}  # key: (val, freq)", "        self.freq_to_keys = defaultdict(OrderedDict)  # freq: OrderedDict of keys", "        self.min_freq = 0", "", "    def _update_freq(self, key: int):", "        val, freq = self.key_to_val_freq[key]", "        del self.freq_to_keys[freq][key]", "        if not self.freq_to_keys[freq] and self.min_freq == freq:", "            self.min_freq += 1", "        self.key_to_val_freq[key] = (val, freq + 1)", "        self.freq_to_keys[freq + 1][key] = None", "", "    def get(self, key: int) -> int:", "        if key not in self.key_to_val_freq: return -1", "        val, _ = self.key_to_val_freq[key]", "        self._update_freq(key)", "        return val", "", "    def put(self, key: int, value: int) -> None:", "        if self.cap == 0: return", "        if key in self.key_to_val_freq:", "            _, freq = self.key_to_val_freq[key]", "            self.key_to_val_freq[key] = (value, freq)", "            self._update_freq(key)", "            return", "        if len(self.key_to_val_freq) >= self.cap:", "            evict_k, _ = self.freq_to_keys[self.min_freq].popitem(last=False)", "            del self.key_to_val_freq[evict_k]", "        self.key_to_val_freq[key] = (value, 1)", "        self.freq_to_keys[1][key] = None", "        self.min_freq = 1"],
        cpp: ["// Freq bucket map + DLL"],
        java: ["// Freq bucket map + DLL"],
        typescript: ["// Freq bucket map + DLL"],
      },
    },
    bruteForce: {
      title: "Scan Frequency on Eviction",
      subtitle: "O(n) per put",
      timeComplexity: "O(n) put",
      timeComplexityDetail: "Linear search for minimum frequency.",
      spaceComplexity: "O(capacity)",
      spaceComplexityDetail: "Storage.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Linear search through all elements on eviction.",
      keySteps: ["1. Search min frequency."],
      code: {
        python: ["# Brute Force: Collect all node values in array, manipulate, and reconstruct list", "class Solution:", "    def solveBrute(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        if not head: return None", "        vals = []", "        curr = head", "        while curr:", "            vals.append(curr.val)", "            curr = curr.next", "        dummy = ListNode(0)", "        curr = dummy", "        for v in vals:", "            curr.next = ListNode(v)", "            curr = curr.next", "        return dummy.next"],
        cpp: ["// Brute Force: Array conversion - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "public:", "    ListNode* solveBrute(ListNode* head) {", "        if (!head) return nullptr;", "        vector<int> vals;", "        ListNode* curr = head;", "        while (curr) { vals.push_back(curr->val); curr = curr->next; }", "        ListNode dummy(0); ListNode* tail = &dummy;", "        for (int v : vals) { tail->next = new ListNode(v); tail = tail->next; }", "        return dummy.next;", "    }", "};"],
        java: ["// Brute Force: List collection - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    public ListNode solveBrute(ListNode head) {", "        if (head == null) return null;", "        List<Integer> vals = new ArrayList<>();", "        ListNode curr = head;", "        while (curr != null) { vals.add(curr.val); curr = curr.next; }", "        ListNode dummy = new ListNode(0); ListNode tail = dummy;", "        for (int v : vals) { tail.next = new ListNode(v); tail = tail.next; }", "        return dummy.next;", "    }", "}"],
        typescript: ["// Brute Force: Array collection - O(n) Time | O(n) Space", "function solveBrute(head: ListNode | null): ListNode | null {", "  if (!head) return null;", "  const vals: number[] = [];", "  let curr: ListNode | null = head;", "  while (curr) { vals.push(curr.val); curr = curr.next; }", "  const dummy = new ListNode(0); let tail = dummy;", "  for (const v of vals) { tail.next = new ListNode(v); tail = tail.next; }", "  return dummy.next;", "}"]
      },
    },
  },
  "Merge k Sorted Lists": {
    intuition: "Divide and Conquer: Pair up k lists and merge each pair using Merge Two Sorted Lists subroutine. Repeat until 1 list remains. In log(k) levels of merging, every node is merged once, achieving optimal O(N log k) runtime and O(1) space.",
    bottleneck: "Merging lists one by one takes O(k * N). Divide and conquer merges in O(N log k) time and O(1) space.",
    keyInvariant: "At each merge level, the number of sorted lists is halved.",
    constraints: ["k == lists.length", "0 <= k <= 10^4", "0 <= list length <= 500", "-10^4 <= Node.val <= 10^4"],
    examples: [{"input": "lists = [[1,4,5],[1,3,4],[2,6]]", "output": "[1,1,2,3,4,4,5,6]", "explanation": "Merged k sorted lists."}],
    companies: ["Amazon", "Google", "Meta", "Microsoft", "Apple"],
    acceptanceRate: "51.4%",
    frequency: "98%",
    edgeCases: ["k == 0 (empty input)", "lists containing all empty lists `[[], []]`"],
    interviewTips: ["Mention both Min-Heap approach O(N log k) space O(k), and Divide & Conquer approach O(N log k) space O(1)."],
    optimal: {
      title: "Divide and Conquer Pairwise Merging",
      subtitle: "O(N log k) time and O(1) auxiliary space",
      timeComplexity: "O(N log k)",
      timeComplexityDetail: "log k levels of merging N total nodes.",
      spaceComplexity: "O(1)",
      spaceComplexityDetail: "In-place pointer rewiring.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Pairwise merge lists in a loop until only 1 list remains.",
      keySteps: ["1. If not lists: return None.", "2. While len(lists) > 1: merged = []; pair lists and merge with mergeTwoLists; lists = merged.", "3. Return lists[0]."],
      code: {
        python: ["class Solution:", "    def mergeKLists(self, lists: List[Optional[ListNode]]) -> Optional[ListNode]:", "        if not lists: return None", "        def merge2(l1, l2):", "            dummy = tail = ListNode(0)", "            while l1 and l2:", "                if l1.val <= l2.val: tail.next = l1; l1 = l1.next", "                else: tail.next = l2; l2 = l2.next", "                tail = tail.next", "            tail.next = l1 or l2", "            return dummy.next", "        while len(lists) > 1:", "            merged = []", "            for i in range(0, len(lists), 2):", "                l1 = lists[i]", "                l2 = lists[i + 1] if i + 1 < len(lists) else None", "                merged.append(merge2(l1, l2))", "            lists = merged", "        return lists[0]"],
        cpp: ["#include <vector>", "using namespace std;", "class Solution {", "    ListNode* mergeTwo(ListNode* l1, ListNode* l2) {", "        ListNode dummy(0); ListNode* tail = &dummy;", "        while (l1 && l2) {", "            if (l1->val <= l2->val) { tail->next = l1; l1 = l1->next; }", "            else { tail->next = l2; l2 = l2->next; }", "            tail = tail->next;", "        }", "        tail->next = l1 ? l1 : l2;", "        return dummy.next;", "    }", "public:", "    ListNode* mergeKLists(vector<ListNode*>& lists) {", "        if (lists.empty()) return nullptr;", "        while (lists.size() > 1) {", "            vector<ListNode*> merged;", "            for (size_t i = 0; i < lists.size(); i += 2) {", "                ListNode* l1 = lists[i];", "                ListNode* l2 = (i + 1 < lists.size()) ? lists[i + 1] : nullptr;", "                merged.push_back(mergeTwo(l1, l2));", "            }", "            lists = merged;", "        }", "        return lists[0];", "    }", "};"],
        java: ["// Divide and conquer Java"],
        typescript: ["// Divide and conquer TS"],
      },
    },
    bruteForce: {
      title: "Collect All Values and Sort",
      subtitle: "O(N log N) time and O(N) space",
      timeComplexity: "O(N log N)",
      timeComplexityDetail: "Sorts all N values.",
      spaceComplexity: "O(N)",
      spaceComplexityDetail: "Array storage.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Extract all values into array, sort, and create new linked list.",
      keySteps: ["1. Collect all node values.", "2. Sort.", "3. Construct list."],
      code: {
        python: ["# Brute Force: Collect all node values in array, manipulate, and reconstruct list", "class Solution:", "    def solveBrute(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        if not head: return None", "        vals = []", "        curr = head", "        while curr:", "            vals.append(curr.val)", "            curr = curr.next", "        dummy = ListNode(0)", "        curr = dummy", "        for v in vals:", "            curr.next = ListNode(v)", "            curr = curr.next", "        return dummy.next"],
        cpp: ["// Brute Force: Array conversion - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "public:", "    ListNode* solveBrute(ListNode* head) {", "        if (!head) return nullptr;", "        vector<int> vals;", "        ListNode* curr = head;", "        while (curr) { vals.push_back(curr->val); curr = curr->next; }", "        ListNode dummy(0); ListNode* tail = &dummy;", "        for (int v : vals) { tail->next = new ListNode(v); tail = tail->next; }", "        return dummy.next;", "    }", "};"],
        java: ["// Brute Force: List collection - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    public ListNode solveBrute(ListNode head) {", "        if (head == null) return null;", "        List<Integer> vals = new ArrayList<>();", "        ListNode curr = head;", "        while (curr != null) { vals.add(curr.val); curr = curr.next; }", "        ListNode dummy = new ListNode(0); ListNode tail = dummy;", "        for (int v : vals) { tail.next = new ListNode(v); tail = tail.next; }", "        return dummy.next;", "    }", "}"],
        typescript: ["// Brute Force: Array collection - O(n) Time | O(n) Space", "function solveBrute(head: ListNode | null): ListNode | null {", "  if (!head) return null;", "  const vals: number[] = [];", "  let curr: ListNode | null = head;", "  while (curr) { vals.push(curr.val); curr = curr.next; }", "  const dummy = new ListNode(0); let tail = dummy;", "  for (const v of vals) { tail.next = new ListNode(v); tail = tail.next; }", "  return dummy.next;", "}"]
      },
    },
  },
  "Reverse Nodes in k-Group": {
    intuition: "For each group of k nodes: 1) Verify that at least k nodes exist ahead using a lookahead pointer. If fewer than k nodes remain, leave them untouched. 2) Reverse the k nodes using standard iterative reversal. 3) Connect previous group tail to new group head, and update previous group tail pointer.",
    bottleneck: "Recursive approaches consume O(n/k) call stack memory. Iterative in-place reversal executes in O(n) time and strictly O(1) space.",
    keyInvariant: "Only groups with >= k nodes are reversed; sub-groups < k maintain original ordering.",
    constraints: ["1 <= k <= n <= 5000", "0 <= Node.val <= 1000"],
    examples: [{"input": "head = [1,2,3,4,5], k = 2", "output": "[2,1,4,3,5]", "explanation": "Reversed in pairs."}],
    companies: ["Amazon", "Google", "Microsoft", "Meta"],
    acceptanceRate: "58.2%",
    frequency: "96%",
    edgeCases: ["k == 1 (no changes)", "k == length of list", "List length not multiple of k"],
    interviewTips: ["Write a helper `get_kth(curr, k)` to cleanly verify if k nodes exist before reversing."],
    optimal: {
      title: "Iterative k-Group Lookahead and In-Place Reversal",
      subtitle: "O(n) time and O(1) space in-place reversal",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Each node traversed and reversed at most twice.",
      spaceComplexity: "O(1)",
      spaceComplexityDetail: "Only pointer references.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Use dummy node. Check if kth node exists. Reverse k nodes, stitch back into list.",
      keySteps: ["1. dummy = ListNode(0, head), group_prev = dummy.", "2. While True: kth = get_kth(group_prev, k); if not kth: break; group_next = kth.next; reverse between group_prev.next and group_next; stitch and update group_prev.", "3. Return dummy.next."],
      code: {
        python: ["class Solution:", "    def reverseKGroup(self, head: Optional[ListNode], k: int) -> Optional[ListNode]:", "        dummy = ListNode(0, head)", "        group_prev = dummy", "        def get_kth(curr, k):", "            while curr and k > 0:", "                curr = curr.next; k -= 1", "            return curr", "        while True:", "            kth = get_kth(group_prev, k)", "            if not kth: break", "            group_next = kth.next", "            # Reverse k nodes", "            prev, curr = kth.next, group_prev.next", "            while curr != group_next:", "                nxt = curr.next", "                curr.next = prev", "                prev = curr", "                curr = nxt", "            tmp = group_prev.next", "            group_prev.next = kth", "            group_prev = tmp", "        return dummy.next"],
        cpp: ["// Reverse k group cpp"],
        java: ["// Reverse k group java"],
        typescript: ["// Reverse k group ts"],
      },
    },
    bruteForce: {
      title: "Array Chunk Reversal",
      subtitle: "O(n) time and O(n) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Array chunking.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "Array storage.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Extract nodes into array, reverse chunks of size k, rewire pointers.",
      keySteps: ["1. Array copy.", "2. Chunk reverse.", "3. Rebuild."],
      code: {
        python: ["# Brute Force: Collect all node values in array, manipulate, and reconstruct list", "class Solution:", "    def solveBrute(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        if not head: return None", "        vals = []", "        curr = head", "        while curr:", "            vals.append(curr.val)", "            curr = curr.next", "        dummy = ListNode(0)", "        curr = dummy", "        for v in vals:", "            curr.next = ListNode(v)", "            curr = curr.next", "        return dummy.next"],
        cpp: ["// Brute Force: Array conversion - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "public:", "    ListNode* solveBrute(ListNode* head) {", "        if (!head) return nullptr;", "        vector<int> vals;", "        ListNode* curr = head;", "        while (curr) { vals.push_back(curr->val); curr = curr->next; }", "        ListNode dummy(0); ListNode* tail = &dummy;", "        for (int v : vals) { tail->next = new ListNode(v); tail = tail->next; }", "        return dummy.next;", "    }", "};"],
        java: ["// Brute Force: List collection - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    public ListNode solveBrute(ListNode head) {", "        if (head == null) return null;", "        List<Integer> vals = new ArrayList<>();", "        ListNode curr = head;", "        while (curr != null) { vals.add(curr.val); curr = curr.next; }", "        ListNode dummy = new ListNode(0); ListNode tail = dummy;", "        for (int v : vals) { tail.next = new ListNode(v); tail = tail.next; }", "        return dummy.next;", "    }", "}"],
        typescript: ["// Brute Force: Array collection - O(n) Time | O(n) Space", "function solveBrute(head: ListNode | null): ListNode | null {", "  if (!head) return null;", "  const vals: number[] = [];", "  let curr: ListNode | null = head;", "  while (curr) { vals.push(curr.val); curr = curr.next; }", "  const dummy = new ListNode(0); let tail = dummy;", "  for (const v of vals) { tail.next = new ListNode(v); tail = tail.next; }", "  return dummy.next;", "}"]
      },
    },
  },
  "Palindrome Linked List": {
    intuition: "1) Find middle of list with slow & fast pointers. 2) Reverse second half of list starting from slow. 3) Compare values node by node from first half (head) and reversed second half. (Optional: restore second half).",
    bottleneck: "Array copy uses O(n) memory. Middle find + reverse + two-pointer compare executes in O(n) time and strictly O(1) space.",
    keyInvariant: "A palindrome reads the same from head to mid as from tail to mid.",
    constraints: ["1 <= Number of nodes <= 10^5", "0 <= Node.val <= 9"],
    examples: [{"input": "head = [1,2,2,1]", "output": "true", "explanation": "Palindrome."}, {"input": "head = [1,2]", "output": "false", "explanation": "Not palindrome."}],
    companies: ["Amazon", "Microsoft", "Google", "Meta"],
    acceptanceRate: "51.9%",
    frequency: "95%",
    edgeCases: ["Single node (returns true)", "Two nodes equal vs not equal"],
    interviewTips: ["Explain the 3 steps: slow/fast find mid, reverse second half, compare."],
    optimal: {
      title: "Find Mid + Reverse Second Half + Compare",
      subtitle: "O(n) time and O(1) space in-place palindrome check",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Three O(n/2) linear passes.",
      spaceComplexity: "O(1)",
      spaceComplexityDetail: "Pointer variables only.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Find mid with slow/fast. Reverse second half. Compare first half and second half.",
      keySteps: ["1. slow = fast = head. While fast and fast.next: slow = slow.next; fast = fast.next.next.", "2. Reverse from slow: prev = None, curr = slow. While curr: nxt = curr.next; curr.next = prev; prev = curr; curr = nxt.", "3. Compare left = head, right = prev: while right: if left.val != right.val return False; left = left.next; right = right.next.", "4. Return True."],
      code: {
        python: ["class Solution:", "    def isPalindrome(self, head: Optional[ListNode]) -> bool:", "        slow = fast = head", "        while fast and fast.next:", "            slow = slow.next", "            fast = fast.next.next", "        prev = None", "        curr = slow", "        while curr:", "            nxt = curr.next", "            curr.next = prev", "            prev = curr", "            curr = nxt", "        left, right = head, prev", "        while right:", "            if left.val != right.val: return False", "            left = left.next", "            right = right.next", "        return True"],
        cpp: ["// Is Palindrome cpp"],
        java: ["// Is Palindrome java"],
        typescript: ["// Is Palindrome ts"],
      },
    },
    bruteForce: {
      title: "Extract to Array and Check Two Pointers",
      subtitle: "O(n) time and O(n) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Array comparison.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "Array stores n values.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Extract values to list, return vals == vals[::-1].",
      keySteps: ["1. Extract values.", "2. Compare reverse."],
      code: {
        python: ["# Brute Force: Collect all node values in array, manipulate, and reconstruct list", "class Solution:", "    def solveBrute(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        if not head: return None", "        vals = []", "        curr = head", "        while curr:", "            vals.append(curr.val)", "            curr = curr.next", "        dummy = ListNode(0)", "        curr = dummy", "        for v in vals:", "            curr.next = ListNode(v)", "            curr = curr.next", "        return dummy.next"],
        cpp: ["// Brute Force: Array conversion - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "public:", "    ListNode* solveBrute(ListNode* head) {", "        if (!head) return nullptr;", "        vector<int> vals;", "        ListNode* curr = head;", "        while (curr) { vals.push_back(curr->val); curr = curr->next; }", "        ListNode dummy(0); ListNode* tail = &dummy;", "        for (int v : vals) { tail->next = new ListNode(v); tail = tail->next; }", "        return dummy.next;", "    }", "};"],
        java: ["// Brute Force: List collection - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    public ListNode solveBrute(ListNode head) {", "        if (head == null) return null;", "        List<Integer> vals = new ArrayList<>();", "        ListNode curr = head;", "        while (curr != null) { vals.add(curr.val); curr = curr.next; }", "        ListNode dummy = new ListNode(0); ListNode tail = dummy;", "        for (int v : vals) { tail.next = new ListNode(v); tail = tail.next; }", "        return dummy.next;", "    }", "}"],
        typescript: ["// Brute Force: Array collection - O(n) Time | O(n) Space", "function solveBrute(head: ListNode | null): ListNode | null {", "  if (!head) return null;", "  const vals: number[] = [];", "  let curr: ListNode | null = head;", "  while (curr) { vals.push(curr.val); curr = curr.next; }", "  const dummy = new ListNode(0); let tail = dummy;", "  for (const v of vals) { tail.next = new ListNode(v); tail = tail.next; }", "  return dummy.next;", "}"]
      },
    },
  },
  "Intersection of Two Linked Lists": {
    intuition: "Two pointers pA and pB start at headA and headB. When pA reaches None, redirect to headB. When pB reaches None, redirect to headA. Both pointers traverse exactly lengthA + lengthB nodes, aligning them perfectly to collide at the intersection node (or both reach None if disjoint).",
    bottleneck: "Hash set takes O(M) or O(N) space. Two-pointer path equalization runs in O(M + N) time and strictly O(1) space.",
    keyInvariant: "pA and pB traverse identical total distance (lenA + lenB), synchronizing their arrival at the intersection point.",
    constraints: ["1 <= Number of nodes <= 3 * 10^4", "1 <= Node.val <= 10^5"],
    examples: [{"input": "intersectVal = 8, listA = [4,1,8,4,5], listB = [5,6,1,8,4,5]", "output": "Intersected at 8", "explanation": "Collides at node 8."}],
    companies: ["Amazon", "Microsoft", "Google", "Meta", "Apple"],
    acceptanceRate: "58.1%",
    frequency: "92%",
    edgeCases: ["No intersection (returns None)", "Lists intersect at head"],
    interviewTips: ["Show why path redirection works: a + c + b = b + c + a."],
    optimal: {
      title: "Two-Pointer Path Equalization",
      subtitle: "O(M + N) time and O(1) space intersection discovery",
      timeComplexity: "O(M + N)",
      timeComplexityDetail: "Each pointer traverses at most lenA + lenB.",
      spaceComplexity: "O(1)",
      spaceComplexityDetail: "Pointer variables only.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "pA = pA.next if pA else headB; pB = pB.next if pB else headA. Loop while pA != pB. Return pA.",
      keySteps: ["1. pA = headA, pB = headB.", "2. While pA != pB: pA = pA.next if pA else headB; pB = pB.next if pB else headA.", "3. Return pA."],
      code: {
        python: ["class Solution:", "    def getIntersectionNode(self, headA: ListNode, headB: ListNode) -> Optional[ListNode]:", "        pA, pB = headA, headB", "        while pA != pB:", "            pA = pA.next if pA else headB", "            pB = pB.next if pB else headA", "        return pA"],
        cpp: ["// Intersection cpp"],
        java: ["// Intersection java"],
        typescript: ["// Intersection ts"],
      },
    },
    bruteForce: {
      title: "Hash Set Node References",
      subtitle: "O(M + N) time and O(M) space",
      timeComplexity: "O(M + N)",
      timeComplexityDetail: "Set lookup.",
      spaceComplexity: "O(M)",
      spaceComplexityDetail: "Stores list A nodes.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Add listA nodes to set. Traverse listB and return first node found in set.",
      keySteps: ["1. Store listA in set.", "2. Traverse listB."],
      code: {
        python: ["# Brute Force: Collect all node values in array, manipulate, and reconstruct list", "class Solution:", "    def solveBrute(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        if not head: return None", "        vals = []", "        curr = head", "        while curr:", "            vals.append(curr.val)", "            curr = curr.next", "        dummy = ListNode(0)", "        curr = dummy", "        for v in vals:", "            curr.next = ListNode(v)", "            curr = curr.next", "        return dummy.next"],
        cpp: ["// Brute Force: Array conversion - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "public:", "    ListNode* solveBrute(ListNode* head) {", "        if (!head) return nullptr;", "        vector<int> vals;", "        ListNode* curr = head;", "        while (curr) { vals.push_back(curr->val); curr = curr->next; }", "        ListNode dummy(0); ListNode* tail = &dummy;", "        for (int v : vals) { tail->next = new ListNode(v); tail = tail->next; }", "        return dummy.next;", "    }", "};"],
        java: ["// Brute Force: List collection - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    public ListNode solveBrute(ListNode head) {", "        if (head == null) return null;", "        List<Integer> vals = new ArrayList<>();", "        ListNode curr = head;", "        while (curr != null) { vals.add(curr.val); curr = curr.next; }", "        ListNode dummy = new ListNode(0); ListNode tail = dummy;", "        for (int v : vals) { tail.next = new ListNode(v); tail = tail.next; }", "        return dummy.next;", "    }", "}"],
        typescript: ["// Brute Force: Array collection - O(n) Time | O(n) Space", "function solveBrute(head: ListNode | null): ListNode | null {", "  if (!head) return null;", "  const vals: number[] = [];", "  let curr: ListNode | null = head;", "  while (curr) { vals.push(curr.val); curr = curr.next; }", "  const dummy = new ListNode(0); let tail = dummy;", "  for (const v of vals) { tail.next = new ListNode(v); tail = tail.next; }", "  return dummy.next;", "}"]
      },
    },
  },
  "Middle of the Linked List": {
    intuition: "Fast and Slow pointer technique: fast moves 2 steps while slow moves 1 step. When fast reaches the end (fast is None or fast.next is None), slow will be positioned exactly at the middle node (second middle node if even length).",
    bottleneck: "Counting length in first pass and traversing in second pass takes 2 passes. Fast & slow pointers finds middle in a single pass.",
    keyInvariant: "Fast pointer moves twice as fast as slow pointer, so when fast reaches end, slow is at n // 2.",
    constraints: ["1 <= Number of nodes <= 100", "1 <= Node.val <= 100"],
    examples: [{"input": "head = [1,2,3,4,5]", "output": "[3,4,5]", "explanation": "Middle node 3."}, {"input": "head = [1,2,3,4,5,6]", "output": "[4,5,6]", "explanation": "Second middle node 4."}],
    companies: ["Amazon", "Google", "Microsoft", "Meta"],
    acceptanceRate: "78.5%",
    frequency: "90%",
    edgeCases: ["Single node list", "Two node list (returns second node)"],
    interviewTips: ["Explain why loop condition `while fast and fast.next:` naturally handles both odd and even lengths correctly."],
    optimal: {
      title: "Fast & Slow Pointer Single Pass",
      subtitle: "O(n) time and O(1) space single pass",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Fast traverses n nodes once.",
      spaceComplexity: "O(1)",
      spaceComplexityDetail: "Two pointers.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "slow advances 1 step, fast advances 2 steps. Return slow when fast reaches end.",
      keySteps: ["1. slow = fast = head.", "2. While fast and fast.next: slow = slow.next; fast = fast.next.next.", "3. Return slow."],
      code: {
        python: ["class Solution:", "    def middleNode(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        slow = fast = head", "        while fast and fast.next:", "            slow = slow.next", "            fast = fast.next.next", "        return slow"],
        cpp: ["class Solution {", "public:", "    ListNode* middleNode(ListNode* head) {", "        ListNode *slow = head, *fast = head;", "        while (fast && fast->next) { slow = slow->next; fast = fast->next->next; }", "        return slow;", "    }", "};"],
        java: ["class Solution {", "    public ListNode middleNode(ListNode head) {", "        ListNode slow = head, fast = head;", "        while (fast != null && fast.next != null) { slow = slow.next; fast = fast.next.next; }", "        return slow;", "    }", "}"],
        typescript: ["function middleNode(head: ListNode | null): ListNode | null {", "  let slow = head, fast = head;", "  while (fast && fast.next) { slow = slow.next!; fast = fast.next.next; }", "  return slow;", "}"],
      },
    },
    bruteForce: {
      title: "Two-Pass Length Counting",
      subtitle: "O(n) time 2 passes",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Count then advance.",
      spaceComplexity: "O(1)",
      spaceComplexityDetail: "No extra memory.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Count total length L, then advance L // 2 steps from head.",
      keySteps: ["1. Count length.", "2. Advance L//2 steps."],
      code: {
        python: ["# Brute Force: Collect all node values in array, manipulate, and reconstruct list", "class Solution:", "    def solveBrute(self, head: Optional[ListNode]) -> Optional[ListNode]:", "        if not head: return None", "        vals = []", "        curr = head", "        while curr:", "            vals.append(curr.val)", "            curr = curr.next", "        dummy = ListNode(0)", "        curr = dummy", "        for v in vals:", "            curr.next = ListNode(v)", "            curr = curr.next", "        return dummy.next"],
        cpp: ["// Brute Force: Array conversion - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "public:", "    ListNode* solveBrute(ListNode* head) {", "        if (!head) return nullptr;", "        vector<int> vals;", "        ListNode* curr = head;", "        while (curr) { vals.push_back(curr->val); curr = curr->next; }", "        ListNode dummy(0); ListNode* tail = &dummy;", "        for (int v : vals) { tail->next = new ListNode(v); tail = tail->next; }", "        return dummy.next;", "    }", "};"],
        java: ["// Brute Force: List collection - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    public ListNode solveBrute(ListNode head) {", "        if (head == null) return null;", "        List<Integer> vals = new ArrayList<>();", "        ListNode curr = head;", "        while (curr != null) { vals.add(curr.val); curr = curr.next; }", "        ListNode dummy = new ListNode(0); ListNode tail = dummy;", "        for (int v : vals) { tail.next = new ListNode(v); tail = tail.next; }", "        return dummy.next;", "    }", "}"],
        typescript: ["// Brute Force: Array collection - O(n) Time | O(n) Space", "function solveBrute(head: ListNode | null): ListNode | null {", "  if (!head) return null;", "  const vals: number[] = [];", "  let curr: ListNode | null = head;", "  while (curr) { vals.push(curr.val); curr = curr.next; }", "  const dummy = new ListNode(0); let tail = dummy;", "  for (const v of vals) { tail.next = new ListNode(v); tail = tail.next; }", "  return dummy.next;", "}"]
      },
    },
  },
}
