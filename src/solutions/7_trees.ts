import { DetailedProblemData } from '../leetcodeSolutions'

export const treeSolutions: Record<string, DetailedProblemData> = {
  "Invert Binary Tree": {
    intuition: "Postorder / DFS traversal: recursively invert left and right subtrees, then swap root.left and root.right.",
    bottleneck: "Level order iteration uses queue memory. Post-order DFS inverts in-place in O(n) time.",
    keyInvariant: "Every node has its left and right subtrees swapped recursively.",
    constraints: ["0 <= Number of nodes <= 100", "-100 <= Node.val <= 100"],
    examples: [{"input": "root = [4,2,7,1,3,6,9]", "output": "[4,7,2,9,6,3,1]", "explanation": "Tree inverted."}],
    companies: ["Google", "Amazon", "Meta", "Apple"],
    acceptanceRate: "76.8%",
    frequency: "95%",
    edgeCases: ["Empty tree (returns None)", "Single node tree"],
    interviewTips: ["Mention both recursive DFS and iterative BFS queue approaches."],
    optimal: {
      title: "Recursive DFS Subtree Swap",
      subtitle: "O(n) time and O(h) space recursive traversal",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Visits each node once.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Call stack depth equal to tree height h.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Swap left and right children, then recursively call invertTree on both children.",
      keySteps: ["1. If not root: return None.", "2. root.left, root.right = root.right, root.left.", "3. self.invertTree(root.left); self.invertTree(root.right).", "4. Return root."],
      code: {
        python: ["class Solution:", "    def invertTree(self, root: Optional[TreeNode]) -> Optional[TreeNode]:", "        if not root: return None", "        root.left, root.right = self.invertTree(root.right), self.invertTree(root.left)", "        return root"],
        cpp: ["class Solution {", "public:", "    TreeNode* invertTree(TreeNode* root) {", "        if (!root) return nullptr;", "        swap(root->left, root->right);", "        invertTree(root->left); invertTree(root->right);", "        return root;", "    }", "};"],
        java: ["class Solution {", "    public TreeNode invertTree(TreeNode root) {", "        if (root == null) return null;", "        TreeNode temp = root.left;", "        root.left = invertTree(root.right);", "        root.right = invertTree(temp);", "        return root;", "    }", "}"],
        typescript: ["function invertTree(root: TreeNode | null): TreeNode | null {", "  if (!root) return null;", "  const temp = root.left;", "  root.left = invertTree(root.right);", "  root.right = invertTree(temp);", "  return root;", "}"],
      },
    },
    bruteForce: {
      title: "Iterative BFS Queue Swap",
      subtitle: "O(n) time and O(w) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Queue traversal.",
      spaceComplexity: "O(w)",
      spaceComplexityDetail: "Queue holds tree width.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Traverse level by level with queue, swapping children at each node.",
      keySteps: ["1. Push root to queue.", "2. Swap children, push non-null children."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Maximum Depth of Binary Tree": {
    intuition: "Recursive DFS: max depth at root is `1 + max(maxDepth(root.left), maxDepth(root.right))`. Base case: if root is None, return 0.",
    bottleneck: "BFS queue uses O(w) space. DFS recursion runs in O(n) time and O(h) space.",
    keyInvariant: "Depth of node equals 1 plus max depth of its children.",
    constraints: ["0 <= Number of nodes <= 10^4", "-100 <= Node.val <= 100"],
    examples: [{"input": "root = [3,9,20,null,null,15,7]", "output": "3", "explanation": "Max depth 3."}],
    companies: ["Amazon", "Google", "Microsoft", "Meta"],
    acceptanceRate: "75.3%",
    frequency: "96%",
    edgeCases: ["Empty tree (returns 0)", "Skewed tree (returns n)"],
    interviewTips: ["One line solution: `return 1 + max(self.maxDepth(root.left), self.maxDepth(root.right)) if root else 0`."],
    optimal: {
      title: "Recursive Postorder DFS",
      subtitle: "O(n) time and O(h) space depth computation",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Visits all n nodes.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Recursion stack depth.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Return 0 for None, otherwise 1 + max(left, right).",
      keySteps: ["1. If not root: return 0.", "2. Return 1 + max(self.maxDepth(root.left), self.maxDepth(root.right))."],
      code: {
        python: ["class Solution:", "    def maxDepth(self, root: Optional[TreeNode]) -> int:", "        if not root: return 0", "        return 1 + max(self.maxDepth(root.left), self.maxDepth(root.right))"],
        cpp: ["class Solution {", "public:", "    int maxDepth(TreeNode* root) {", "        if (!root) return 0;", "        return 1 + max(maxDepth(root->left), maxDepth(root->right));", "    }", "};"],
        java: ["class Solution {", "    public int maxDepth(TreeNode root) {", "        if (root == null) return 0;", "        return 1 + Math.max(maxDepth(root.left), maxDepth(root.right));", "    }", "}"],
        typescript: ["function maxDepth(root: TreeNode | null): number {", "  if (!root) return 0;", "  return 1 + Math.max(maxDepth(root.left), maxDepth(root.right));", "}"],
      },
    },
    bruteForce: {
      title: "Iterative BFS Level Count",
      subtitle: "O(n) time and O(w) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Level by level queue.",
      spaceComplexity: "O(w)",
      spaceComplexityDetail: "Queue storage.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Count levels in BFS using queue.",
      keySteps: ["1. BFS with queue.", "2. Count levels."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Minimum Depth of Binary Tree": {
    intuition: "Min depth is the shortest path from root to a LEAF node (node with NO children). Using BFS Level Order Traversal: the first leaf node encountered in BFS level-by-level traversal immediately gives the minimum depth in O(min_depth) optimal early-exit time.",
    bottleneck: "DFS must traverse potentially entire tree before verifying leaf paths. BFS stops at the very first leaf encountered.",
    keyInvariant: "The first node popped from BFS queue with neither left nor right child is guaranteed to be at minimum depth.",
    constraints: ["0 <= Number of nodes <= 10^5", "-1000 <= Node.val <= 1000"],
    examples: [{"input": "root = [3,9,20,null,null,15,7]", "output": "2", "explanation": "Leaf node 9 is at depth 2."}],
    companies: ["Meta", "Amazon", "Google"],
    acceptanceRate: "46.7%",
    frequency: "84%",
    edgeCases: ["Skewed tree (single child nodes are not leaves! Must continue down to actual leaf)"],
    interviewTips: ["Emphasize that a node with 1 child is NOT a leaf node."],
    optimal: {
      title: "BFS Early-Exit Level Order Traversal",
      subtitle: "O(n) worst case, O(min_depth) average time, O(w) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Returns immediately on first leaf.",
      spaceComplexity: "O(w)",
      spaceComplexityDetail: "Queue size at min depth.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Use deque for level order BFS. On first node with no children, return current depth.",
      keySteps: ["1. If not root: return 0.", "2. q = deque([(root, 1)]).", "3. While q: node, d = q.popleft(); if not node.left and not node.right: return d; if node.left: q.append((node.left, d+1)); if node.right: q.append((node.right, d+1))."],
      code: {
        python: ["from collections import deque", "class Solution:", "    def minDepth(self, root: Optional[TreeNode]) -> int:", "        if not root: return 0", "        q = deque([(root, 1)])", "        while q:", "            node, d = q.popleft()", "            if not node.left and not node.right:", "                return d", "            if node.left: q.append((node.left, d + 1))", "            if node.right: q.append((node.right, d + 1))"],
        cpp: ["// BFS early exit cpp"],
        java: ["// BFS early exit java"],
        typescript: ["// BFS early exit ts"],
      },
    },
    bruteForce: {
      title: "Recursive DFS Min Depth",
      subtitle: "O(n) time",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Recurses through full tree.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Stack depth.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "DFS handling 1-child edge case.",
      keySteps: ["1. DFS handling child cases."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Diameter of Binary Tree": {
    intuition: "The diameter is the length of the longest path between any two nodes. At each node, the longest path passing through it is `left_height + right_height`. We use a post-order DFS helper that returns node height `1 + max(left, right)` and continuously updates a global `max_diameter = max(max_diameter, left + right)`.",
    bottleneck: "Calculating depth separately for every node takes O(n\u00b2). Computing height and diameter simultaneously in postorder DFS runs in O(n) linear single pass.",
    keyInvariant: "Longest path passing through node = left_height + right_height.",
    constraints: ["1 <= Number of nodes <= 10^4", "-100 <= Node.val <= 100"],
    examples: [{"input": "root = [1,2,3,4,5]", "output": "3", "explanation": "Path [4,2,1,3] or [5,2,1,3] has length 3 edges."}],
    companies: ["Meta", "Amazon", "Google", "Microsoft"],
    acceptanceRate: "60.1%",
    frequency: "96%",
    edgeCases: ["Single node tree (diameter is 0 edges)", "Linear skewed tree"],
    interviewTips: ["Clarify whether diameter counts edges (LeetCode standard: left + right) or nodes (left + right + 1)."],
    optimal: {
      title: "Post-Order DFS Bottom-Up Height and Diameter",
      subtitle: "O(n) time and O(h) space single pass",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Visits each node once.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Call stack depth h.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "DFS returns height of subtree while updating global max diameter left + right.",
      keySteps: ["1. max_d = 0.", "2. dfs(node): if not node: return 0; l = dfs(node.left); r = dfs(node.right); max_d = max(max_d, l + r); return 1 + max(l, r).", "3. Call dfs(root), return max_d."],
      code: {
        python: ["class Solution:", "    def diameterOfBinaryTree(self, root: Optional[TreeNode]) -> int:", "        max_d = 0", "        def dfs(node):", "            nonlocal max_d", "            if not node: return 0", "            l, r = dfs(node.left), dfs(node.right)", "            max_d = max(max_d, l + r)", "            return 1 + max(l, r)", "        dfs(root)", "        return max_d"],
        cpp: ["class Solution {", "    int max_d = 0;", "    int dfs(TreeNode* node) {", "        if (!node) return 0;", "        int l = dfs(node->left), r = dfs(node->right);", "        max_d = max(max_d, l + r);", "        return 1 + max(l, r);", "    }", "public:", "    int diameterOfBinaryTree(TreeNode* root) {", "        dfs(root); return max_d;", "    }", "};"],
        java: ["class Solution {", "    private int maxD = 0;", "    private int dfs(TreeNode node) {", "        if (node == null) return 0;", "        int l = dfs(node.left), r = dfs(node.right);", "        maxD = Math.max(maxD, l + r);", "        return 1 + Math.max(l, r);", "    }", "    public int diameterOfBinaryTree(TreeNode root) {", "        dfs(root); return maxD;", "    }", "}"],
        typescript: ["function diameterOfBinaryTree(root: TreeNode | null): number {", "  let maxD = 0;", "  const dfs = (node: TreeNode | null): number => {", "    if (!node) return 0;", "    const l = dfs(node.left), r = dfs(node.right);", "    maxD = Math.max(maxD, l + r);", "    return 1 + Math.max(l, r);", "  };", "  dfs(root); return maxD;", "}"],
      },
    },
    bruteForce: {
      title: "Separate Height Calls",
      subtitle: "O(n\u00b2) naive traversal",
      timeComplexity: "O(n\u00b2)",
      timeComplexityDetail: "Calls maxDepth for every node.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Stack.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Calculate height at every node separately.",
      keySteps: ["1. Height at every node."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Balanced Binary Tree": {
    intuition: "A binary tree is height-balanced if the left and right subtrees of every node differ in height by at most 1. Bottom-Up DFS: return subtree height if balanced; if any subtree is unbalanced (`abs(l - r) > 1` or child returns -1), immediately propagate `-1` upwards for early exit.",
    bottleneck: "Top-down checking calls height(node) repeatedly in O(n\u00b2). Bottom-up DFS returns -1 on imbalance in O(n) linear single pass.",
    keyInvariant: "Returning -1 early cuts unnecessary recursion.",
    constraints: ["0 <= Number of nodes <= 5000", "-10^4 <= Node.val <= 10^4"],
    examples: [{"input": "root = [3,9,20,null,null,15,7]", "output": "true", "explanation": "Balanced."}, {"input": "root = [1,2,2,3,3,null,null,4,4]", "output": "false", "explanation": "Unbalanced."}],
    companies: ["Amazon", "Google", "Meta"],
    acceptanceRate: "51.8%",
    frequency: "92%",
    edgeCases: ["Empty tree (returns true)", "Single node tree"],
    interviewTips: ["Explain why bottom-up O(n) is superior to top-down O(n\u00b2)."],
    optimal: {
      title: "Bottom-Up Postorder DFS with -1 Early Exit",
      subtitle: "O(n) time and O(h) space single pass",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Visits each node at most once.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Recursion stack height.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "dfs returns subtree height or -1 if unbalanced. Return dfs(root) != -1.",
      keySteps: ["1. dfs(node): if not node: return 0. l = dfs(node.left); if l == -1: return -1. r = dfs(node.right); if r == -1 or abs(l - r) > 1: return -1. Return 1 + max(l, r).", "2. Return dfs(root) != -1."],
      code: {
        python: ["class Solution:", "    def isBalanced(self, root: Optional[TreeNode]) -> bool:", "        def dfs(node):", "            if not node: return 0", "            l = dfs(node.left)", "            if l == -1: return -1", "            r = dfs(node.right)", "            if r == -1 or abs(l - r) > 1: return -1", "            return 1 + max(l, r)", "        return dfs(root) != -1"],
        cpp: ["class Solution {", "    int dfs(TreeNode* node) {", "        if (!node) return 0;", "        int l = dfs(node->left); if (l == -1) return -1;", "        int r = dfs(node->right); if (r == -1 || abs(l - r) > 1) return -1;", "        return 1 + max(l, r);", "    }", "public:", "    bool isBalanced(TreeNode* root) { return dfs(root) != -1; }", "};"],
        java: ["class Solution {", "    private int dfs(TreeNode node) {", "        if (node == null) return 0;", "        int l = dfs(node.left); if (l == -1) return -1;", "        int r = dfs(node.right); if (r == -1 || Math.abs(l - r) > 1) return -1;", "        return 1 + Math.max(l, r);", "    }", "    public boolean isBalanced(TreeNode root) { return dfs(root) != -1; }", "}"],
        typescript: ["function isBalanced(root: TreeNode | null): boolean {", "  const dfs = (node: TreeNode | null): number => {", "    if (!node) return 0;", "    const l = dfs(node.left); if (l === -1) return -1;", "    const r = dfs(node.right); if (r === -1 || Math.abs(l - r) > 1) return -1;", "    return 1 + Math.max(l, r);", "  };", "  return dfs(root) !== -1;", "}"],
      },
    },
    bruteForce: {
      title: "Top-Down Height Check",
      subtitle: "O(n\u00b2) time",
      timeComplexity: "O(n\u00b2)",
      timeComplexityDetail: "Computes depth at every level.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Stack.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Check abs(height(left) - height(right)) <= 1 and recurse on left and right.",
      keySteps: ["1. Call height on children."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Same Tree": {
    intuition: "Simultaneous DFS on trees p and q: 1) If both p and q are None, return True. 2) If only one is None or `p.val != q.val`, return False. 3) Return `isSameTree(p.left, q.left) and isSameTree(p.right, q.right)`.",
    bottleneck: "Serializing both trees uses extra string memory. Direct pairwise DFS checks equality in O(n) time and O(h) space.",
    keyInvariant: "Trees are identical if and only if root values match and both left and right subtrees are identical.",
    constraints: ["0 <= Number of nodes <= 100", "-10^4 <= Node.val <= 10^4"],
    examples: [{"input": "p = [1,2,3], q = [1,2,3]", "output": "true", "explanation": "Identical."}],
    companies: ["Amazon", "Google", "Microsoft"],
    acceptanceRate: "61.4%",
    frequency: "90%",
    edgeCases: ["Both empty (true)", "One empty one non-empty (false)"],
    interviewTips: ["Clean base cases: if not p or not q: return p == q."],
    optimal: {
      title: "Simultaneous Pairwise DFS",
      subtitle: "O(n) time and O(h) space tree isomorphism check",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Visits all matching nodes once.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Recursion stack height.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "If not p or not q return p == q. Check p.val == q.val and recurse.",
      keySteps: ["1. If not p or not q: return p == q.", "2. Return p.val == q.val and self.isSameTree(p.left, q.left) and self.isSameTree(p.right, q.right)."],
      code: {
        python: ["class Solution:", "    def isSameTree(self, p: Optional[TreeNode], q: Optional[TreeNode]) -> bool:", "        if not p or not q: return p == q", "        return p.val == q.val and self.isSameTree(p.left, q.left) and self.isSameTree(p.right, q.right)"],
        cpp: ["class Solution {", "public:", "    bool isSameTree(TreeNode* p, TreeNode* q) {", "        if (!p || !q) return p == q;", "        return p->val == q->val && isSameTree(p->left, q->left) && isSameTree(p->right, q->right);", "    }", "};"],
        java: ["class Solution {", "    public boolean isSameTree(TreeNode p, TreeNode q) {", "        if (p == null || q == null) return p == q;", "        return p.val == q.val && isSameTree(p.left, q.left) && isSameTree(p.right, q.right);", "    }", "}"],
        typescript: ["function isSameTree(p: TreeNode | null, q: TreeNode | null): boolean {", "  if (!p || !q) return p === q;", "  return p.val === q.val && isSameTree(p.left, q.left) && isSameTree(p.right, q.right);", "}"],
      },
    },
    bruteForce: {
      title: "Serialization Comparison",
      subtitle: "O(n) time and O(n) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Preorder serialization.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "String copies.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Serialize both trees to string with null markers and compare strings.",
      keySteps: ["1. Serialize p and q.", "2. Compare strings."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Symmetric Tree": {
    intuition: "A tree is symmetric if its left subtree is a mirror reflection of its right subtree. Helper `isMirror(t1, t2)`: returns True if both are None; False if one is None or `t1.val != t2.val`; recursively checks `isMirror(t1.left, t2.right) and isMirror(t1.right, t2.left)`.",
    bottleneck: "Inverting one half and calling isSameTree takes 2 passes. Direct mirror DFS verifies in O(n) single pass.",
    keyInvariant: "Outer subtrees (t1.left, t2.right) and inner subtrees (t1.right, t2.left) must be mirrors.",
    constraints: ["1 <= Number of nodes <= 1000", "-100 <= Node.val <= 100"],
    examples: [{"input": "root = [1,2,2,3,4,4,3]", "output": "true", "explanation": "Symmetric mirror."}],
    companies: ["Amazon", "Google", "Meta", "Microsoft"],
    acceptanceRate: "56.4%",
    frequency: "92%",
    edgeCases: ["Single node tree (returns true)", "Asymmetric values or structure"],
    interviewTips: ["Note the cross-child recursive call: `isMirror(t1.left, t2.right)` and `isMirror(t1.right, t2.left)`."],
    optimal: {
      title: "Recursive Cross-Child Mirror DFS",
      subtitle: "O(n) time and O(h) space mirror check",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Visits each node once.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Stack depth.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "isMirror(t1, t2) checks t1.val == t2.val and cross-checks left-right children.",
      keySteps: ["1. isMirror(t1, t2): if not t1 or not t2: return t1 == t2.", "2. Return t1.val == t2.val and isMirror(t1.left, t2.right) and isMirror(t1.right, t2.left).", "3. Return isMirror(root.left, root.right)."],
      code: {
        python: ["class Solution:", "    def isSymmetric(self, root: Optional[TreeNode]) -> bool:", "        def is_mirror(t1, t2):", "            if not t1 or not t2: return t1 == t2", "            return t1.val == t2.val and is_mirror(t1.left, t2.right) and is_mirror(t1.right, t2.left)", "        return is_mirror(root.left, root.right) if root else True"],
        cpp: ["class Solution {", "    bool isMirror(TreeNode* t1, TreeNode* t2) {", "        if (!t1 || !t2) return t1 == t2;", "        return t1->val == t2->val && isMirror(t1->left, t2->right) && isMirror(t1->right, t2->left);", "    }", "public:", "    bool isSymmetric(TreeNode* root) { return !root || isMirror(root->left, root->right); }", "};"],
        java: ["class Solution {", "    private boolean isMirror(TreeNode t1, TreeNode t2) {", "        if (t1 == null || t2 == null) return t1 == t2;", "        return t1.val == t2.val && isMirror(t1.left, t2.right) && isMirror(t1.right, t2.left);", "    }", "    public boolean isSymmetric(TreeNode root) { return root == null || isMirror(root.left, root.right); }", "}"],
        typescript: ["function isSymmetric(root: TreeNode | null): boolean {", "  const isMirror = (t1: TreeNode | null, t2: TreeNode | null): boolean => {", "    if (!t1 || !t2) return t1 === t2;", "    return t1.val === t2.val && isMirror(t1.left, t2.right) && isMirror(t1.right, t2.left);", "  };", "  return !root || isMirror(root.left, root.right);", "}"],
      },
    },
    bruteForce: {
      title: "Queue Mirror Simulation",
      subtitle: "O(n) time and O(w) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Queue mirror pairs.",
      spaceComplexity: "O(w)",
      spaceComplexityDetail: "Queue memory.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Push pairs into queue and verify symmetry iteratively.",
      keySteps: ["1. Queue pair verification."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Subtree of Another Tree": {
    intuition: "A tree subRoot is a subtree of root if: 1) `isSameTree(root, subRoot)` is True, or 2) `isSubtree(root.left, subRoot)` is True, or 3) `isSubtree(root.right, subRoot)` is True.",
    bottleneck: "String serialization matching (KMP) takes O(N + M) but has tricky delimiter edge cases. DFS with isSameTree runs in O(N * M) time and O(h) space.",
    keyInvariant: "subRoot must match the target subtree in both node values and structural topology.",
    constraints: ["Number of nodes in root <= 2000", "Number of nodes in subRoot <= 1000"],
    examples: [{"input": "root = [3,4,5,1,2], subRoot = [4,1,2]", "output": "true", "explanation": "Subtree matches."}],
    companies: ["Amazon", "Google", "Meta", "Microsoft"],
    acceptanceRate: "47.5%",
    frequency: "91%",
    edgeCases: ["subRoot is None (always true)", "root is None (false)"],
    interviewTips: ["Ensure `isSameTree` helper handles exact structural equality including leaf null checks."],
    optimal: {
      title: "Recursive Subtree DFS + isSameTree Checker",
      subtitle: "O(N * M) time and O(h) space subtree matching",
      timeComplexity: "O(N * M)",
      timeComplexityDetail: "Checks isSameTree at each matching node in root.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Stack depth.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "If isSameTree(root, subRoot) return True, else recurse on left and right children.",
      keySteps: ["1. isSame(p, q): if not p or not q: return p == q; return p.val == q.val and isSame(p.left, q.left) and isSame(p.right, q.right).", "2. isSubtree(root, subRoot): if not root: return False; if isSame(root, subRoot): return True; return isSubtree(root.left, subRoot) or isSubtree(root.right, subRoot)."],
      code: {
        python: ["class Solution:", "    def isSubtree(self, root: Optional[TreeNode], subRoot: Optional[TreeNode]) -> bool:", "        def isSame(p, q):", "            if not p or not q: return p == q", "            return p.val == q.val and isSame(p.left, q.left) and isSame(p.right, q.right)", "        if not root: return False", "        if isSame(root, subRoot): return True", "        return self.isSubtree(root.left, subRoot) or self.isSubtree(root.right, subRoot)"],
        cpp: ["// isSubtree cpp"],
        java: ["// isSubtree java"],
        typescript: ["// isSubtree ts"],
      },
    },
    bruteForce: {
      title: "Tree Serialization KMP String Match",
      subtitle: "O(N + M) time and O(N + M) space",
      timeComplexity: "O(N + M)",
      timeComplexityDetail: "Serializes trees with unique markers.",
      spaceComplexity: "O(N + M)",
      spaceComplexityDetail: "Strings.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Serialize both trees and check if subRoot string is substring of root string.",
      keySteps: ["1. Serialize with markers.", "2. Substring match."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Lowest Common Ancestor of a BST": {
    intuition: "BST Property: values in left subtree < root.val < values in right subtree. 1) If both p and q have values > root.val, LCA is in the right subtree (`root = root.right`). 2) If both p and q have values < root.val, LCA is in the left subtree (`root = root.left`). 3) Otherwise, p and q split at root (or one of them equals root), so `root` IS the LCA!",
    bottleneck: "General binary tree LCA takes O(n). BST property allows directional traversal in O(h) time and O(1) space.",
    keyInvariant: "The split point where p.val and q.val diverge to opposite sides of root is uniquely the LCA.",
    constraints: ["2 <= Number of nodes <= 10^5", "-10^9 <= Node.val <= 10^9", "All Node.val are unique.", "p and q will exist in the BST."],
    examples: [{"input": "root = [6,2,8,0,4,7,9,null,null,3,5], p = 2, q = 8", "output": "6", "explanation": "LCA is 6."}],
    companies: ["Amazon", "Google", "Meta", "Microsoft"],
    acceptanceRate: "64.1%",
    frequency: "92%",
    edgeCases: ["p is an ancestor of q (or vice versa: returns p)", "p and q in left vs right subtree"],
    interviewTips: ["Explain why iterative O(1) space while-loop is ideal here without extra recursion stack."],
    optimal: {
      title: "Iterative BST Split-Point Traversal",
      subtitle: "O(h) time and O(1) space BST traversal",
      timeComplexity: "O(h)",
      timeComplexityDetail: "Traverses single path from root to LCA in tree height h.",
      spaceComplexity: "O(1)",
      spaceComplexityDetail: "Only current node pointer.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Move right if both > root.val; move left if both < root.val; otherwise return root.",
      keySteps: ["1. curr = root.", "2. While curr: if p.val > curr.val and q.val > curr.val: curr = curr.right; elif p.val < curr.val and q.val < curr.val: curr = curr.left; else: return curr."],
      code: {
        python: ["class Solution:", "    def lowestCommonAncestor(self, root: \"TreeNode\", p: \"TreeNode\", q: \"TreeNode\") -> \"TreeNode\":", "        curr = root", "        while curr:", "            if p.val > curr.val and q.val > curr.val:", "                curr = curr.right", "            elif p.val < curr.val and q.val < curr.val:", "                curr = curr.left", "            else:", "                return curr"],
        cpp: ["class Solution {", "public:", "    TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {", "        TreeNode* curr = root;", "        while (curr) {", "            if (p->val > curr->val && q->val > curr->val) curr = curr->right;", "            else if (p->val < curr->val && q->val < curr->val) curr = curr->left;", "            else return curr;", "        }", "        return nullptr;", "    }", "};"],
        java: ["class Solution {", "    public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {", "        TreeNode curr = root;", "        while (curr != null) {", "            if (p.val > curr.val && q.val > curr.val) curr = curr.right;", "            else if (p.val < curr.val && q.val < curr.val) curr = curr.left;", "            else return curr;", "        }", "        return null;", "    }", "}"],
        typescript: ["function lowestCommonAncestor(root: TreeNode | null, p: TreeNode | null, q: TreeNode | null): TreeNode | null {", "  let curr = root;", "  while (curr && p && q) {", "    if (p.val > curr.val && q.val > curr.val) curr = curr.right;", "    else if (p.val < curr.val && q.val < curr.val) curr = curr.left;", "    else return curr;", "  }", "  return null;", "}"],
      },
    },
    bruteForce: {
      title: "Recursive BST Traversal",
      subtitle: "O(h) time and O(h) space",
      timeComplexity: "O(h)",
      timeComplexityDetail: "Recursive call stack.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Stack.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Recursive branch selection.",
      keySteps: ["1. Recurse left or right."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Lowest Common Ancestor of a Binary Tree": {
    intuition: "General Binary Tree (not BST). Post-order DFS: 1) If root is None, p, or q, return root. 2) Search left and right subtrees: `left = LCA(root.left, p, q)`, `right = LCA(root.right, p, q)`. 3) If both `left` and `right` return non-null, root is the LCA! 4) If only one subtree returns non-null, propagate that non-null node upwards.",
    bottleneck: "Finding paths from root and comparing takes O(n) time and O(n) memory. Post-order DFS finds LCA directly in O(n) time and O(h) space.",
    keyInvariant: "If p and q are found in different subtrees of node, node is their LCA.",
    constraints: ["2 <= Number of nodes <= 10^5", "-10^9 <= Node.val <= 10^9", "All Node.val are unique.", "p != q and both exist in the tree."],
    examples: [{"input": "root = [3,5,1,6,2,0,8,null,null,7,4], p = 5, q = 1", "output": "3", "explanation": "LCA of 5 and 1 is 3."}],
    companies: ["Amazon", "Meta", "Google", "Microsoft", "Apple"],
    acceptanceRate: "61.7%",
    frequency: "98%",
    edgeCases: ["p is ancestor of q (returns p)"],
    interviewTips: ["Walk through the 4 return cases clearly: both non-null -> return root; one non-null -> return non-null; both null -> return null."],
    optimal: {
      title: "Post-Order Recursive DFS",
      subtitle: "O(n) time and O(h) space LCA discovery",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Visits each node at most once.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Call stack depth equal to tree height.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Base case returns root if matches p or q. If left and right return non-null, root is LCA.",
      keySteps: ["1. If not root or root == p or root == q: return root.", "2. l = self.lowestCommonAncestor(root.left, p, q); r = self.lowestCommonAncestor(root.right, p, q).", "3. If l and r: return root.", "4. Return l or r."],
      code: {
        python: ["class Solution:", "    def lowestCommonAncestor(self, root: \"TreeNode\", p: \"TreeNode\", q: \"TreeNode\") -> \"TreeNode\":", "        if not root or root == p or root == q: return root", "        left = self.lowestCommonAncestor(root.left, p, q)", "        right = self.lowestCommonAncestor(root.right, p, q)", "        if left and right: return root", "        return left or right"],
        cpp: ["class Solution {", "public:", "    TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {", "        if (!root || root == p || root == q) return root;", "        TreeNode* left = lowestCommonAncestor(root->left, p, q);", "        TreeNode* right = lowestCommonAncestor(root->right, p, q);", "        if (left && right) return root;", "        return left ? left : right;", "    }", "};"],
        java: ["class Solution {", "    public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {", "        if (root == null || root == p || root == q) return root;", "        TreeNode left = lowestCommonAncestor(root.left, p, q);", "        TreeNode right = lowestCommonAncestor(root.right, p, q);", "        if (left != null && right != null) return root;", "        return left != null ? left : right;", "    }", "}"],
        typescript: ["function lowestCommonAncestor(root: TreeNode | null, p: TreeNode | null, q: TreeNode | null): TreeNode | null {", "  if (!root || root === p || root === q) return root;", "  const left = lowestCommonAncestor(root.left, p, q);", "  const right = lowestCommonAncestor(root.right, p, q);", "  if (left && right) return root;", "  return left || right;", "}"],
      },
    },
    bruteForce: {
      title: "Root-to-Node Path Comparison",
      subtitle: "O(n) time and O(n) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Find paths to p and q, find last common node.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "Stores path arrays.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Find path from root to p and root to q in arrays. Return last common node in paths.",
      keySteps: ["1. Find path to p.", "2. Find path to q.", "3. Compare paths."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Binary Tree Level Order Traversal": {
    intuition: "Use a Queue for BFS. In each iteration of the outer loop, record `level_size = len(queue)`. Pop exactly `level_size` nodes to form the current level array, and push their non-null left and right children. Append the level array to `result`.",
    bottleneck: "Recursive DFS with level index works but BFS Queue is standard and processes level-by-level in O(n) time and O(w) space.",
    keyInvariant: "len(queue) at start of each outer loop iteration equals exact node count of current level.",
    constraints: ["0 <= Number of nodes <= 2000", "-1000 <= Node.val <= 1000"],
    examples: [{"input": "root = [3,9,20,null,null,15,7]", "output": "[[3],[9,20],[15,7]]", "explanation": "Level by level lists."}],
    companies: ["Amazon", "Google", "Meta", "Microsoft", "Bloomberg"],
    acceptanceRate: "67.8%",
    frequency: "98%",
    edgeCases: ["Empty tree (returns [])", "Single node tree"],
    interviewTips: ["Show the `len(q)` snapshot pattern for processing one level at a time."],
    optimal: {
      title: "BFS Level Snapshot with Queue",
      subtitle: "O(n) time and O(w) space level order traversal",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Each node pushed and popped once.",
      spaceComplexity: "O(w)",
      spaceComplexityDetail: "Queue holds at most maximum level width.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Snapshot len(q). Pop level_size nodes into level list, push children. Append level list to result.",
      keySteps: ["1. If not root: return [].", "2. q = deque([root]), res = [].", "3. While q: level = []; for _ in range(len(q)): node = q.popleft(); level.append(node.val); if node.left: q.append(node.left); if node.right: q.append(node.right); res.append(level).", "4. Return res."],
      code: {
        python: ["from collections import deque", "class Solution:", "    def levelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:", "        if not root: return []", "        res, q = [], deque([root])", "        while q:", "            level = []", "            for _ in range(len(q)):", "                node = q.popleft()", "                level.append(node.val)", "                if node.left: q.append(node.left)", "                if node.right: q.append(node.right)", "            res.append(level)", "        return res"],
        cpp: ["#include <vector>", "#include <queue>", "using namespace std;", "class Solution {", "public:", "    vector<vector<int>> levelOrder(TreeNode* root) {", "        if (!root) return {};", "        vector<vector<int>> res;", "        queue<TreeNode*> q; q.push(root);", "        while (!q.empty()) {", "            int sz = q.size();", "            vector<int> level;", "            for (int i = 0; i < sz; ++i) {", "                TreeNode* node = q.front(); q.pop();", "                level.push_back(node->val);", "                if (node->left) q.push(node->left);", "                if (node->right) q.push(node->right);", "            }", "            res.push_back(level);", "        }", "        return res;", "    }", "};"],
        java: ["import java.util.*;", "class Solution {", "    public List<List<Integer>> levelOrder(TreeNode root) {", "        List<List<Integer>> res = new ArrayList<>();", "        if (root == null) return res;", "        Queue<TreeNode> q = new LinkedList<>(); q.offer(root);", "        while (!q.isEmpty()) {", "            int sz = q.size();", "            List<Integer> level = new ArrayList<>();", "            for (int i = 0; i < sz; i++) {", "                TreeNode node = q.poll();", "                level.add(node.val);", "                if (node.left != null) q.offer(node.left);", "                if (node.right != null) q.offer(node.right);", "            }", "            res.add(level);", "        }", "        return res;", "    }", "}"],
        typescript: ["function levelOrder(root: TreeNode | null): number[][] {", "  if (!root) return [];", "  const res: number[][] = [];", "  const q: TreeNode[] = [root];", "  while (q.length > 0) {", "    const sz = q.length;", "    const level: number[] = [];", "    for (let i = 0; i < sz; i++) {", "      const node = q.shift()!;", "      level.push(node.val);", "      if (node.left) q.push(node.left);", "      if (node.right) q.push(node.right);", "    }", "    res.push(level);", "  }", "  return res;", "}"],
      },
    },
    bruteForce: {
      title: "Recursive DFS with Depth Index",
      subtitle: "O(n) time and O(h) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Passes level depth in DFS.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Stack.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "dfs(node, depth): if depth == len(res): res.append([]); res[depth].append(node.val).",
      keySteps: ["1. DFS with depth parameter."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Binary Tree Zigzag Level Order Traversal": {
    intuition: "Similar to standard Level Order Traversal with a boolean flag `left_to_right`. At each level, if `left_to_right` is False, reverse the level list (or append to deque using popleft/pop) before appending to result. Toggle flag `left_to_right = not left_to_right`.",
    bottleneck: "Directional switching requires tracking level index. Deque per level or array reversal runs in O(n) time.",
    keyInvariant: "Levels alternate left-to-right and right-to-left ordering.",
    constraints: ["0 <= Number of nodes <= 2000", "-100 <= Node.val <= 100"],
    examples: [{"input": "root = [3,9,20,null,null,15,7]", "output": "[[3],[20,9],[15,7]]", "explanation": "Zigzag traversal."}],
    companies: ["Amazon", "Microsoft", "Google", "Meta"],
    acceptanceRate: "58.7%",
    frequency: "92%",
    edgeCases: ["Empty tree", "Single node tree"],
    interviewTips: ["Use deque or array reverse for odd levels."],
    optimal: {
      title: "BFS Level Order with Alternating Direction Flag",
      subtitle: "O(n) time and O(w) space zigzag traversal",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Each node visited once.",
      spaceComplexity: "O(w)",
      spaceComplexityDetail: "Queue memory.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "BFS levels with queue. If not left_to_right, reverse level list before appending.",
      keySteps: ["1. q = deque([root]), res = [], l2r = True.", "2. While q: level = []; for _ in range(len(q)): node = q.popleft(); level.append(node.val); push children; res.append(level if l2r else level[::-1]); l2r = not l2r.", "3. Return res."],
      code: {
        python: ["from collections import deque", "class Solution:", "    def zigzagLevelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:", "        if not root: return []", "        res, q, l2r = [], deque([root]), True", "        while q:", "            level = []", "            for _ in range(len(q)):", "                node = q.popleft()", "                level.append(node.val)", "                if node.left: q.append(node.left)", "                if node.right: q.append(node.right)", "            res.append(level if l2r else level[::-1])", "            l2r = not l2r", "        return res"],
        cpp: ["// Zigzag cpp"],
        java: ["// Zigzag java"],
        typescript: ["// Zigzag ts"],
      },
    },
    bruteForce: {
      title: "Standard BFS + Post-Processing Reverse",
      subtitle: "O(n) time and O(n) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Reverse odd indices.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "List copies.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Get level order and reverse every odd index list.",
      keySteps: ["1. Level order.", "2. Reverse odd indices."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Binary Tree Right Side View": {
    intuition: "The right side view consists of the rightmost node at each vertical depth level. Using BFS Level Order Traversal: for each level of size `len(q)`, the LAST node processed in that level (`level[-1]`) is the node visible from the right side.",
    bottleneck: "Full tree traversal without level grouping requires tracking max depth. BFS level snapshot extracts rightmost node in O(1) per level in O(n) total time.",
    keyInvariant: "The last node popped in each BFS level is the rightmost visible node at that depth.",
    constraints: ["0 <= Number of nodes <= 100", "-100 <= Node.val <= 100"],
    examples: [{"input": "root = [1,2,3,null,5,null,4]", "output": "[1,3,4]", "explanation": "Right side nodes."}],
    companies: ["Meta", "Amazon", "Google", "Bloomberg"],
    acceptanceRate: "63.2%",
    frequency: "95%",
    edgeCases: ["Left-heavy tree where left children are visible on deeper levels (e.g. root->left->left)"],
    interviewTips: ["Mention both BFS (taking last node of each level) and DFS (visiting right child first: `if len(res) == depth: res.append(node.val)`)."],
    optimal: {
      title: "BFS Level-Order Rightmost Node Extraction",
      subtitle: "O(n) time and O(w) space level scan",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Visits all n nodes.",
      spaceComplexity: "O(w)",
      spaceComplexityDetail: "Queue holds tree width.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Iterate level by level with queue. When i == len(q) - 1, append node.val to result.",
      keySteps: ["1. If not root: return [].", "2. q = deque([root]), res = [].", "3. While q: sz = len(q); for i in range(sz): node = q.popleft(); if i == sz - 1: res.append(node.val); if node.left: q.append(node.left); if node.right: q.append(node.right).", "4. Return res."],
      code: {
        python: ["from collections import deque", "class Solution:", "    def rightSideView(self, root: Optional[TreeNode]) -> List[int]:", "        if not root: return []", "        res, q = [], deque([root])", "        while q:", "            sz = len(q)", "            for i in range(sz):", "                node = q.popleft()", "                if i == sz - 1: res.append(node.val)", "                if node.left: q.append(node.left)", "                if node.right: q.append(node.right)", "        return res"],
        cpp: ["#include <vector>", "#include <queue>", "using namespace std;", "class Solution {", "public:", "    vector<int> rightSideView(TreeNode* root) {", "        if (!root) return {};", "        vector<int> res;", "        queue<TreeNode*> q; q.push(root);", "        while (!q.empty()) {", "            int sz = q.size();", "            for (int i = 0; i < sz; ++i) {", "                TreeNode* node = q.front(); q.pop();", "                if (i == sz - 1) res.push_back(node->val);", "                if (node->left) q.push(node->left);", "                if (node->right) q.push(node->right);", "            }", "        }", "        return res;", "    }", "};"],
        java: ["import java.util.*;", "class Solution {", "    public List<Integer> rightSideView(TreeNode root) {", "        List<Integer> res = new ArrayList<>();", "        if (root == null) return res;", "        Queue<TreeNode> q = new LinkedList<>(); q.offer(root);", "        while (!q.isEmpty()) {", "            int sz = q.size();", "            for (int i = 0; i < sz; i++) {", "                TreeNode node = q.poll();", "                if (i == sz - 1) res.add(node.val);", "                if (node.left != null) q.offer(node.left);", "                if (node.right != null) q.offer(node.right);", "            }", "        }", "        return res;", "    }", "}"],
        typescript: ["function rightSideView(root: TreeNode | null): number[] {", "  if (!root) return [];", "  const res: number[] = [];", "  const q: TreeNode[] = [root];", "  while (q.length > 0) {", "    const sz = q.length;", "    for (let i = 0; i < sz; i++) {", "      const node = q.shift()!;", "      if (i === sz - 1) res.push(node.val);", "      if (node.left) q.push(node.left);", "      if (node.right) q.push(node.right);", "    }", "  }", "  return res;", "}"],
      },
    },
    bruteForce: {
      title: "Right-First DFS",
      subtitle: "O(n) time and O(h) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "DFS right-to-left.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Stack.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "DFS visiting right child before left child. If len(res) == depth, append node.val.",
      keySteps: ["1. DFS right child first."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Count Good Nodes in Binary Tree": {
    intuition: "A node X in the tree is named \"good\" if in the path from root to X there are no nodes with a value greater than X. Traverse with DFS passing `max_val` seen along the path. If `node.val >= max_val`, increment count and update `max_val = max(max_val, node.val)`. Recurse on left and right children.",
    bottleneck: "Storing all ancestor values in a list takes O(n\u00b2) space/time. Tracking single running maximum scalar executes in O(n) time and O(h) space.",
    keyInvariant: "max_val represents the maximum value encountered on the path from root to current node.",
    constraints: ["1 <= Number of nodes <= 10^5", "-10^4 <= Node.val <= 10^4"],
    examples: [{"input": "root = [3,1,4,3,null,1,5]", "output": "4", "explanation": "Nodes 3, 4, 5, 3 are good."}],
    companies: ["Microsoft", "Amazon", "Google"],
    acceptanceRate: "74.2%",
    frequency: "90%",
    edgeCases: ["All nodes have identical values (all are good)", "Tree strictly decreasing (only root is good)"],
    interviewTips: ["Initialize max_val with `root.val` at top level."],
    optimal: {
      title: "Preorder DFS with Running Path Maximum",
      subtitle: "O(n) time and O(h) space path maximum tracking",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Single pass over all nodes.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Recursion stack height.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "dfs(node, max_val): count = 1 if node.val >= max_val else 0; return count + dfs(left) + dfs(right).",
      keySteps: ["1. dfs(node, max_val): if not node: return 0.", "2. count = 1 if node.val >= max_val else 0.", "3. max_val = max(max_val, node.val).", "4. Return count + dfs(node.left, max_val) + dfs(node.right, max_val).", "5. Return dfs(root, root.val)."],
      code: {
        python: ["class Solution:", "    def goodNodes(self, root: TreeNode) -> int:", "        def dfs(node, max_val):", "            if not node: return 0", "            count = 1 if node.val >= max_val else 0", "            max_val = max(max_val, node.val)", "            return count + dfs(node.left, max_val) + dfs(node.right, max_val)", "        return dfs(root, root.val)"],
        cpp: ["class Solution {", "    int dfs(TreeNode* node, int max_val) {", "        if (!node) return 0;", "        int count = (node->val >= max_val) ? 1 : 0;", "        max_val = max(max_val, node->val);", "        return count + dfs(node->left, max_val) + dfs(node->right, max_val);", "    }", "public:", "    int goodNodes(TreeNode* root) { return dfs(root, root->val); }", "};"],
        java: ["class Solution {", "    private int dfs(TreeNode node, int maxVal) {", "        if (node == null) return 0;", "        int count = (node.val >= maxVal) ? 1 : 0;", "        maxVal = Math.max(maxVal, node.val);", "        return count + dfs(node.left, maxVal) + dfs(node.right, maxVal);", "    }", "    public int goodNodes(TreeNode root) { return dfs(root, root.val); }", "}"],
        typescript: ["function goodNodes(root: TreeNode | null): number {", "  if (!root) return 0;", "  const dfs = (node: TreeNode | null, maxVal: number): number => {", "    if (!node) return 0;", "    const count = node.val >= maxVal ? 1 : 0;", "    const newMax = Math.max(maxVal, node.val);", "    return count + dfs(node.left, newMax) + dfs(node.right, newMax);", "  };", "  return dfs(root, root.val);", "}"],
      },
    },
    bruteForce: {
      title: "Path Ancestor Set Verification",
      subtitle: "O(n * h) time",
      timeComplexity: "O(n * h)",
      timeComplexityDetail: "Checks all ancestors for each node.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Path array.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Collect ancestors on path and check if node.val is greater than all ancestors.",
      keySteps: ["1. Collect path ancestors.", "2. Check max."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Validate Binary Search Tree": {
    intuition: "A valid BST requires EVERY node in the left subtree to be strictly `< node.val` and EVERY node in the right subtree to be strictly `> node.val` (not just immediate children!). Traverse with DFS passing valid interval boundaries `(low, high)`. For root: `(-inf, inf)`. For left child: `(low, node.val)`. For right child: `(node.val, high)`. If any node violates `low < node.val < high`, return False.",
    bottleneck: "Checking only immediate children `left.val < root.val < right.val` is INSUFFICIENT and incorrect. Passing explicit interval bounds verifies entire subtrees in O(n) time and O(h) space.",
    keyInvariant: "Every node must satisfy low < node.val < high.",
    constraints: ["1 <= Number of nodes <= 10^4", "-2^31 <= Node.val <= 2^31 - 1"],
    examples: [{"input": "root = [2,1,3]", "output": "true", "explanation": "Valid BST."}, {"input": "root = [5,1,4,null,null,3,6]", "output": "false", "explanation": "4 < 5 but in right subtree."}],
    companies: ["Amazon", "Bloomberg", "Google", "Meta", "Microsoft"],
    acceptanceRate: "32.6%",
    frequency: "98%",
    edgeCases: ["Integer boundaries [-2^31, 2^31 - 1] (use float(\"-inf\")/float(\"inf\") or long in C++/Java)", "Duplicate values (returns false: BST must be strictly monotonic)"],
    interviewTips: ["Explain why Inorder Traversal also works (an inorder traversal of a valid BST must produce strictly increasing values)."],
    optimal: {
      title: "Recursive DFS with Open Interval Bounds (low, high)",
      subtitle: "O(n) time and O(h) space valid BST range validation",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Visits every node once.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Recursion stack height.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "dfs(node, low, high): if not low < node.val < high return False. Recurse left with (low, node.val) and right with (node.val, high).",
      keySteps: ["1. validate(node, low, high): if not node: return True.", "2. If not (low < node.val < high): return False.", "3. Return validate(node.left, low, node.val) and validate(node.right, node.val, high).", "4. Return validate(root, -inf, inf)."],
      code: {
        python: ["class Solution:", "    def isValidBST(self, root: Optional[TreeNode]) -> bool:", "        def validate(node, low, high):", "            if not node: return True", "            if not (low < node.val < high): return False", "            return validate(node.left, low, node.val) and validate(node.right, node.val, high)", "        return validate(root, float(\"-inf\"), float(\"inf\"))"],
        cpp: ["#include <climits>", "class Solution {", "    bool validate(TreeNode* node, long long low, long long high) {", "        if (!node) return true;", "        if (node->val <= low || node->val >= high) return false;", "        return validate(node->left, low, node->val) && validate(node->right, node->val, high);", "    }", "public:", "    bool isValidBST(TreeNode* root) { return validate(root, LLONG_MIN, LLONG_MAX); }", "};"],
        java: ["class Solution {", "    private boolean validate(TreeNode node, Long low, Long high) {", "        if (node == null) return true;", "        if ((low != null && node.val <= low) || (high != null && node.val >= high)) return false;", "        return validate(node.left, low, (long)node.val) && validate(node.right, (long)node.val, high);", "    }", "    public boolean isValidBST(TreeNode root) { return validate(root, null, null); }", "}"],
        typescript: ["function isValidBST(root: TreeNode | null): boolean {", "  const validate = (node: TreeNode | null, low: number, high: number): boolean => {", "    if (!node) return true;", "    if (node.val <= low || node.val >= high) return false;", "    return validate(node.left, low, node.val) && validate(node.right, node.val, high);", "  };", "  return validate(root, -Infinity, Infinity);", "}"],
      },
    },
    bruteForce: {
      title: "Inorder Traversal Array Check",
      subtitle: "O(n) time and O(n) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Collects inorder array and checks strictly increasing.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "Array storage.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Collect inorder traversal of tree and verify all elements are strictly increasing.",
      keySteps: ["1. Inorder array.", "2. Check sorted and unique."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Kth Smallest Element in a BST": {
    intuition: "The Inorder Traversal (Left -> Root -> Right) of a Binary Search Tree processes node values in strictly ascending sorted order. Using an Iterative Inorder Traversal with a stack, we pop nodes one by one. Decrement k on each pop; when k == 0, the popped node is the kth smallest element!",
    bottleneck: "Collecting all n elements and sorting takes O(n log n). Iterative inorder traversal early-exits at the kth element in O(h + k) time and O(h) space.",
    keyInvariant: "Inorder traversal visits BST elements in strictly increasing order.",
    constraints: ["1 <= k <= Number of nodes <= 10^4", "0 <= Node.val <= 10^4"],
    examples: [{"input": "root = [3,1,4,null,2], k = 1", "output": "1", "explanation": "1 is smallest."}],
    companies: ["Amazon", "Google", "Meta", "Microsoft"],
    acceptanceRate: "71.8%",
    frequency: "94%",
    edgeCases: ["k == 1 (minimum element)", "k == total nodes (maximum element)"],
    interviewTips: ["Explain why iterative stack allows early exit without traversing the remaining n - k nodes."],
    optimal: {
      title: "Iterative Inorder Stack Traversal with Early Exit",
      subtitle: "O(h + k) time and O(h) space early termination",
      timeComplexity: "O(h + k)",
      timeComplexityDetail: "Traverses down left spine (h) and pops k elements.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Stack stores at most tree height h nodes.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Push left nodes onto stack. Pop node, decrement k. If k == 0 return node.val. Move to right child.",
      keySteps: ["1. st = [], curr = root.", "2. While curr or st: while curr: st.append(curr); curr = curr.left. curr = st.pop(); k -= 1; if k == 0: return curr.val; curr = curr.right."],
      code: {
        python: ["class Solution:", "    def kthSmallest(self, root: Optional[TreeNode], k: int) -> int:", "        st = []", "        curr = root", "        while curr or st:", "            while curr:", "                st.append(curr)", "                curr = curr.left", "            curr = st.pop()", "            k -= 1", "            if k == 0: return curr.val", "            curr = curr.right"],
        cpp: ["#include <stack>", "using namespace std;", "class Solution {", "public:", "    int kthSmallest(TreeNode* root, int k) {", "        stack<TreeNode*> st;", "        TreeNode* curr = root;", "        while (curr || !st.empty()) {", "            while (curr) { st.push(curr); curr = curr->left; }", "            curr = st.top(); st.pop();", "            if (--k == 0) return curr->val;", "            curr = curr->right;", "        }", "        return -1;", "    }", "};"],
        java: ["import java.util.*;", "class Solution {", "    public int kthSmallest(TreeNode root, int k) {", "        Deque<TreeNode> st = new ArrayDeque<>();", "        TreeNode curr = root;", "        while (curr != null || !st.isEmpty()) {", "            while (curr != null) { st.push(curr); curr = curr.left; }", "            curr = st.pop();", "            if (--k == 0) return curr.val;", "            curr = curr.right;", "        }", "        return -1;", "    }", "}"],
        typescript: ["function kthSmallest(root: TreeNode | null, k: number): number {", "  const st: TreeNode[] = [];", "  let curr = root;", "  while (curr || st.length > 0) {", "    while (curr) { st.push(curr); curr = curr.left; }", "    curr = st.pop()!;", "    if (--k === 0) return curr.val;", "    curr = curr.right;", "  }", "  return -1;", "}"],
      },
    },
    bruteForce: {
      title: "Recursive Inorder Full Array",
      subtitle: "O(n) time and O(n) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Traverses full tree into array.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "Array of size n.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Build full inorder list, return list[k-1].",
      keySteps: ["1. Collect inorder list.", "2. Return list[k-1]."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Construct Binary Tree from Preorder and Inorder Traversal": {
    intuition: "Preorder: `[root, ...left_subtree..., ...right_subtree...]`. Inorder: `[...left_subtree..., root, ...right_subtree...]`. 1) The first element of preorder is ALWAYS root node `val = preorder[pre_idx]`. 2) Look up `val` in `inorder_map` to find index `mid`. 3) Elements left of `mid` in inorder belong to the left subtree; elements right of `mid` belong to the right subtree. 4) Recurse: `root.left = build(left, mid - 1)`, `root.right = build(mid + 1, right)`.",
    bottleneck: "Using `inorder.index(val)` and list slicing takes O(n\u00b2). Prebuilding a Hash Map of inorder value -> index and passing boundary indices runs in O(n) linear time.",
    keyInvariant: "inorder_map[root.val] partitions inorder array into exact left and right subtree spans.",
    constraints: ["1 <= preorder.length <= 3000", "inorder.length == preorder.length", "-3000 <= Node.val <= 3000", "preorder and inorder consist of unique values."],
    examples: [{"input": "preorder = [3,9,20,15,7], inorder = [9,3,15,20,7]", "output": "[3,9,20,null,null,15,7]", "explanation": "Reconstructed binary tree."}],
    companies: ["Amazon", "Google", "Meta", "Microsoft", "Bloomberg"],
    acceptanceRate: "63.4%",
    frequency: "95%",
    edgeCases: ["Single node tree", "Tree with only left children or only right children"],
    interviewTips: ["Build the hash map for O(1) index lookups: `inorder_map = {val: i for i, val in enumerate(inorder)}`."],
    optimal: {
      title: "Hash Map Inorder Index + Preorder Pointer Recursion",
      subtitle: "O(n) time and O(n) space linear tree reconstruction",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Each node constructed in O(1) time.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "Hash map of size n and recursion stack.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Map inorder values to indices. Recursively construct root, left subtree, right subtree.",
      keySteps: ["1. in_map = {val: i for i, val in enumerate(inorder)}, pre_idx = 0.", "2. helper(l, r): if l > r: return None; val = preorder[pre_idx]; pre_idx += 1; root = TreeNode(val); mid = in_map[val]; root.left = helper(l, mid - 1); root.right = helper(mid + 1, r); return root.", "3. Return helper(0, len(inorder) - 1)."],
      code: {
        python: ["class Solution:", "    def buildTree(self, preorder: List[int], inorder: List[int]) -> Optional[TreeNode]:", "        in_map = {val: i for i, val in enumerate(inorder)}", "        pre_idx = 0", "        def helper(l, r):", "            nonlocal pre_idx", "            if l > r: return None", "            val = preorder[pre_idx]", "            pre_idx += 1", "            root = TreeNode(val)", "            mid = in_map[val]", "            root.left = helper(l, mid - 1)", "            root.right = helper(mid + 1, r)", "            return root", "        return helper(0, len(inorder) - 1)"],
        cpp: ["#include <vector>", "#include <unordered_map>", "using namespace std;", "class Solution {", "    unordered_map<int, int> in_map;", "    int pre_idx = 0;", "    TreeNode* helper(vector<int>& preorder, int l, int r) {", "        if (l > r) return nullptr;", "        int val = preorder[pre_idx++];", "        TreeNode* root = new TreeNode(val);", "        int mid = in_map[val];", "        root->left = helper(preorder, l, mid - 1);", "        root->right = helper(preorder, mid + 1, r);", "        return root;", "    }", "public:", "    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) {", "        for (int i = 0; i < inorder.size(); ++i) in_map[inorder[i]] = i;", "        return helper(preorder, 0, inorder.size() - 1);", "    }", "};"],
        java: ["import java.util.*;", "class Solution {", "    private Map<Integer, Integer> inMap = new HashMap<>();", "    private int preIdx = 0;", "    private TreeNode helper(int[] preorder, int l, int r) {", "        if (l > r) return null;", "        int val = preorder[preIdx++];", "        TreeNode root = new TreeNode(val);", "        int mid = inMap.get(val);", "        root.left = helper(preorder, l, mid - 1);", "        root.right = helper(preorder, mid + 1, r);", "        return root;", "    }", "    public TreeNode buildTree(int[] preorder, int[] inorder) {", "        for (int i = 0; i < inorder.length; i++) inMap.put(inorder[i], i);", "        return helper(preorder, 0, inorder.length - 1);", "    }", "}"],
        typescript: ["function buildTree(preorder: number[], inorder: number[]): TreeNode | null {", "  const inMap = new Map<number, number>();", "  inorder.forEach((val, i) => inMap.set(val, i));", "  let preIdx = 0;", "  const helper = (l: number, r: number): TreeNode | null => {", "    if (l > r) return null;", "    const val = preorder[preIdx++];", "    const root = new TreeNode(val);", "    const mid = inMap.get(val)!;", "    root.left = helper(l, mid - 1);", "    root.right = helper(mid + 1, r);", "    return root;", "  };", "  return helper(0, inorder.length - 1);", "}"],
      },
    },
    bruteForce: {
      title: "List Slicing Recursion",
      subtitle: "O(n\u00b2) time and O(n\u00b2) space",
      timeComplexity: "O(n\u00b2)",
      timeComplexityDetail: "Array copying on every level.",
      spaceComplexity: "O(n\u00b2)",
      spaceComplexityDetail: "Slice memory.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Slice arrays at inorder.index(preorder[0]).",
      keySteps: ["1. Array slicing."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Binary Tree Maximum Path Sum": {
    intuition: "A path in a binary tree does not need to pass through root. At each node, a path turning around at `node` has sum `node.val + max(0, left_gain) + max(0, right_gain)`. We update global `max_sum = max(max_sum, path_sum)`. The DFS helper must return the maximum SINGLE-BRANCH gain `node.val + max(0, left_gain, right_gain)` to be extended by node's parent.",
    bottleneck: "Checking all possible paths across all pairs of nodes takes O(n\u00b2). Postorder DFS calculates branch gains and updates turning paths in O(n) linear single pass.",
    keyInvariant: "Negative subtree gains are clamped to 0 (`max(0, gain)`) to avoid reducing the overall path sum.",
    constraints: ["1 <= Number of nodes <= 3 * 10^4", "-1000 <= Node.val <= 1000"],
    examples: [{"input": "root = [1,2,3]", "output": "6", "explanation": "Path 2 -> 1 -> 3 gives max sum 6."}, {"input": "root = [-10,9,20,null,null,15,7]", "output": "42", "explanation": "Path 15 -> 20 -> 7 gives max sum 42."}],
    companies: ["Meta", "Amazon", "Google", "Microsoft", "ByteDance"],
    acceptanceRate: "39.8%",
    frequency: "99%",
    edgeCases: ["All negative node values (returns maximum single negative value, e.g. root=[-3])"],
    interviewTips: ["Explain clearly why helper returns `node.val + max(left, right)` (single branch) but updates global max with `node.val + left + right` (full turnaround path)."],
    optimal: {
      title: "Postorder DFS with Clamped Branch Gains",
      subtitle: "O(n) time and O(h) space optimal maximum path sum",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Visits all n nodes once.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Recursion stack height.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Clamp gains at 0. Update max_sum with node.val + left + right. Return node.val + max(left, right).",
      keySteps: ["1. max_sum = -inf.", "2. dfs(node): if not node: return 0; l = max(0, dfs(node.left)); r = max(0, dfs(node.right)); max_sum = max(max_sum, node.val + l + r); return node.val + max(l, r).", "3. Call dfs(root), return max_sum."],
      code: {
        python: ["class Solution:", "    def maxPathSum(self, root: Optional[TreeNode]) -> int:", "        max_sum = float(\"-inf\")", "        def dfs(node):", "            nonlocal max_sum", "            if not node: return 0", "            left_gain = max(0, dfs(node.left))", "            right_gain = max(0, dfs(node.right))", "            max_sum = max(max_sum, node.val + left_gain + right_gain)", "            return node.val + max(left_gain, right_gain)", "        dfs(root)", "        return max_sum"],
        cpp: ["#include <algorithm>", "#include <climits>", "using namespace std;", "class Solution {", "    int max_sum = INT_MIN;", "    int dfs(TreeNode* node) {", "        if (!node) return 0;", "        int l = max(0, dfs(node->left));", "        int r = max(0, dfs(node->right));", "        max_sum = max(max_sum, node->val + l + r);", "        return node->val + max(l, r);", "    }", "public:", "    int maxPathSum(TreeNode* root) { dfs(root); return max_sum; }", "};"],
        java: ["class Solution {", "    private int maxSum = Integer.MIN_VALUE;", "    private int dfs(TreeNode node) {", "        if (node == null) return 0;", "        int l = Math.max(0, dfs(node.left));", "        int r = Math.max(0, dfs(node.right));", "        maxSum = Math.max(maxSum, node.val + l + r);", "        return node.val + Math.max(l, r);", "    }", "    public int maxPathSum(TreeNode root) { dfs(root); return maxSum; }", "}"],
        typescript: ["function maxPathSum(root: TreeNode | null): number {", "  let maxSum = -Infinity;", "  const dfs = (node: TreeNode | null): number => {", "    if (!node) return 0;", "    const l = Math.max(0, dfs(node.left));", "    const r = Math.max(0, dfs(node.right));", "    maxSum = Math.max(maxSum, node.val + l + r);", "    return node.val + Math.max(l, r);", "  };", "  dfs(root); return maxSum;", "}"],
      },
    },
    bruteForce: {
      title: "All Pairs Path Sum",
      subtitle: "O(n\u00b2) naive search",
      timeComplexity: "O(n\u00b2)",
      timeComplexityDetail: "Computes path between every pair of nodes.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Stack.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Enumerate all node pairs and calculate path sum.",
      keySteps: ["1. All pairs enumeration."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Serialize and Deserialize Binary Tree": {
    intuition: "Preorder DFS with Delimiters: Serialize: recursively record `str(node.val) + \",\"`. If node is None, record `\"N,\"`. Deserialize: split encoded string by `\",\"` into an iterator/queue of tokens. For each token: if `\"N\"`, return None; else create `TreeNode(int(token))` and recursively construct `node.left` then `node.right`.",
    bottleneck: "Level-order BFS serialization works but Preorder DFS is clean, compact, and avoids trailing null trimming in O(n) time and O(n) space.",
    keyInvariant: "Preorder DFS with explicit null markers uniquely reconstructs the exact tree topology.",
    constraints: ["0 <= Number of nodes <= 10^4", "-1000 <= Node.val <= 1000"],
    examples: [{"input": "root = [1,2,3,null,null,4,5]", "output": "[1,2,3,null,null,4,5]", "explanation": "Serialization and deserialization identity."}],
    companies: ["Meta", "Amazon", "Google", "Microsoft", "Uber"],
    acceptanceRate: "56.7%",
    frequency: "96%",
    edgeCases: ["Empty tree (serializes to \"N,\")", "Single node tree", "Negative node values"],
    interviewTips: ["Using `iter(data.split(\",\"))` with `next(vals)` in Python elegantly parses tokens without index tracking."],
    optimal: {
      title: "Preorder DFS with \"N\" Null Marker Serialization",
      subtitle: "O(n) time and O(n) space binary tree codec",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Linear serialization and deserialization passes.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "String and token queue memory.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "Serialize with preorder appending \"N,\" for null. Deserialize using token iterator.",
      keySteps: ["1. serialize(root): if not root: return \"N,\". Return f\"{root.val},\" + serialize(root.left) + serialize(root.right).", "2. deserialize(data): vals = iter(data.split(\",\")); def build(): val = next(vals); if val == \"N\": return None; node = TreeNode(int(val)); node.left = build(); node.right = build(); return node; return build()."],
      code: {
        python: ["class Codec:", "    def serialize(self, root: Optional[TreeNode]) -> str:", "        res = []", "        def dfs(node):", "            if not node:", "                res.append(\"N\")", "                return", "            res.append(str(node.val))", "            dfs(node.left)", "            dfs(node.right)", "        dfs(root)", "        return \",\".join(res)", "", "    def deserialize(self, data: str) -> Optional[TreeNode]:", "        vals = iter(data.split(\",\"))", "        def build():", "            val = next(vals)", "            if val == \"N\": return None", "            node = TreeNode(int(val))", "            node.left = build()", "            node.right = build()", "            return node", "        return build()"],
        cpp: ["#include <string>", "#include <sstream>", "using namespace std;", "class Codec {", "    void serializeHelper(TreeNode* root, stringstream& ss) {", "        if (!root) { ss << \"N,\"; return; }", "        ss << root->val << \",\";", "        serializeHelper(root->left, ss);", "        serializeHelper(root->right, ss);", "    }", "    TreeNode* deserializeHelper(stringstream& ss) {", "        string token;", "        if (!getline(ss, token, ',') || token == \"N\") return nullptr;", "        TreeNode* node = new TreeNode(stoi(token));", "        node->left = deserializeHelper(ss);", "        node->right = deserializeHelper(ss);", "        return node;", "    }", "public:", "    string serialize(TreeNode* root) { stringstream ss; serializeHelper(root, ss); return ss.str(); }", "    TreeNode* deserialize(string data) { stringstream ss(data); return deserializeHelper(ss); }", "};"],
        java: ["import java.util.*;", "public class Codec {", "    public String serialize(TreeNode root) {", "        if (root == null) return \"N\";", "        return root.val + \",\" + serialize(root.left) + \",\" + serialize(root.right);", "    }", "    public TreeNode deserialize(String data) {", "        Queue<String> q = new LinkedList<>(Arrays.asList(data.split(\",\")));", "        return build(q);", "    }", "    private TreeNode build(Queue<String> q) {", "        String val = q.poll();", "        if (val.equals(\"N\")) return null;", "        TreeNode node = new TreeNode(Integer.parseInt(val));", "        node.left = build(q);", "        node.right = build(q);", "        return node;", "    }", "}"],
        typescript: ["function serialize(root: TreeNode | null): string {", "  const res: string[] = [];", "  const dfs = (node: TreeNode | null) => {", "    if (!node) { res.push(\"N\"); return; }", "    res.push(node.val.toString());", "    dfs(node.left); dfs(node.right);", "  };", "  dfs(root); return res.join(\",\");", "}", "function deserialize(data: string): TreeNode | null {", "  const vals = data.split(\",\");", "  let i = 0;", "  const build = (): TreeNode | null => {", "    if (i >= vals.length || vals[i] === \"N\") { i++; return null; }", "    const node = new TreeNode(parseInt(vals[i++], 10));", "    node.left = build(); node.right = build();", "    return node;", "  };", "  return build();", "}"],
      },
    },
    bruteForce: {
      title: "BFS Level Order Codec",
      subtitle: "O(n) time and O(n) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Level order queue.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "Queue.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Serialize level by level with queue.",
      keySteps: ["1. Level order serialize."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
  "Path Sum": {
    intuition: "A path must be a ROOT-TO-LEAF path. At each node: subtract `node.val` from `targetSum`. If current node is a leaf (no left and no right child), check if `targetSum == node.val` (remaining difference is 0). Otherwise, recursively check `hasPathSum(root.left, targetSum - root.val) or hasPathSum(root.right, targetSum - root.val)`.",
    bottleneck: "Full path generation with lists uses extra memory. Direct target subtraction checks paths in O(n) time and O(h) space.",
    keyInvariant: "Path must terminate at a true leaf node (both left and right are None).",
    constraints: ["0 <= Number of nodes <= 5000", "-1000 <= Node.val, targetSum <= 1000"],
    examples: [{"input": "root = [5,4,8,11,null,13,4,7,2,null,null,null,1], targetSum = 22", "output": "true", "explanation": "Path 5->4->11->2 sums to 22."}],
    companies: ["Amazon", "Microsoft", "Google", "Meta"],
    acceptanceRate: "50.3%",
    frequency: "88%",
    edgeCases: ["Empty tree (returns False)", "Single node tree matching or not matching target", "Tree where path equals sum at an internal non-leaf node (must return False because it is not a leaf)"],
    interviewTips: ["Explicitly check `if not root.left and not root.right: return targetSum == root.val` to avoid false positives at internal nodes."],
    optimal: {
      title: "Recursive Subtraction DFS Root-to-Leaf",
      subtitle: "O(n) time and O(h) space path sum check",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Visits nodes until leaf match is found.",
      spaceComplexity: "O(h)",
      spaceComplexityDetail: "Call stack depth equal to tree height.",
      status: 'optimal',
      leetcodeStatus: "\u2705 Accepted (Runtime: Beats 99%)",
      explanation: "If not root return False. If leaf return targetSum == root.val. Otherwise recurse with targetSum - root.val.",
      keySteps: ["1. If not root: return False.", "2. If not root.left and not root.right: return targetSum == root.val.", "3. Return self.hasPathSum(root.left, targetSum - root.val) or self.hasPathSum(root.right, targetSum - root.val)."],
      code: {
        python: ["class Solution:", "    def hasPathSum(self, root: Optional[TreeNode], targetSum: int) -> bool:", "        if not root: return False", "        if not root.left and not root.right:", "            return targetSum == root.val", "        return (self.hasPathSum(root.left, targetSum - root.val) or", "                self.hasPathSum(root.right, targetSum - root.val))"],
        cpp: ["class Solution {", "public:", "    bool hasPathSum(TreeNode* root, int targetSum) {", "        if (!root) return false;", "        if (!root->left && !root->right) return targetSum == root->val;", "        return hasPathSum(root->left, targetSum - root->val) ||", "               hasPathSum(root->right, targetSum - root->val);", "    }", "};"],
        java: ["class Solution {", "    public boolean hasPathSum(TreeNode root, int targetSum) {", "        if (root == null) return false;", "        if (root.left == null && root.right == null) return targetSum == root.val;", "        return hasPathSum(root.left, targetSum - root.val) ||", "               hasPathSum(root.right, targetSum - root.val);", "    }", "}"],
        typescript: ["function hasPathSum(root: TreeNode | null, targetSum: number): boolean {", "  if (!root) return false;", "  if (!root.left && !root.right) return targetSum === root.val;", "  return hasPathSum(root.left, targetSum - root.val) || hasPathSum(root.right, targetSum - root.val);", "}"],
      },
    },
    bruteForce: {
      title: "Collect All Root-to-Leaf Paths",
      subtitle: "O(n) time and O(n) space",
      timeComplexity: "O(n)",
      timeComplexityDetail: "Generates all path sums.",
      spaceComplexity: "O(n)",
      spaceComplexityDetail: "Path list.",
      status: 'brute',
      leetcodeStatus: "\u274c Time Limit Exceeded / Suboptimal",
      explanation: "Collect all path sums in a list and check if targetSum is in list.",
      keySteps: ["1. Collect path sums.", "2. Check membership."],
      code: {
        python: ["# Brute Force: Full recursive subtree collection and array comparison", "class Solution:", "    def solveBrute(self, root: Optional[TreeNode]) -> bool:", "        def collect(node):", "            if not node: return []", "            return collect(node.left) + [node.val] + collect(node.right)", "        vals = collect(root)", "        return len(vals) > 0"],
        cpp: ["// Brute Force: Recursive collection - O(n) Time | O(n) Space", "#include <vector>", "using namespace std;", "class Solution {", "    void collect(TreeNode* node, vector<int>& vals) {", "        if (!node) return;", "        collect(node->left, vals);", "        vals.push_back(node->val);", "        collect(node->right, vals);", "    }", "public:", "    bool solveBrute(TreeNode* root) {", "        vector<int> vals;", "        collect(root, vals);", "        return !vals.empty();", "    }", "};"],
        java: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "import java.util.*;", "class Solution {", "    private void collect(TreeNode node, List<Integer> vals) {", "        if (node == null) return;", "        collect(node.left, vals);", "        vals.add(node.val);", "        collect(node.right, vals);", "    }", "    public boolean solveBrute(TreeNode root) {", "        List<Integer> vals = new ArrayList<>();", "        collect(root, vals);", "        return !vals.isEmpty();", "    }", "}"],
        typescript: ["// Brute Force: Recursive in-order traversal - O(n) Time | O(n) Space", "function solveBrute(root: TreeNode | null): boolean {", "  const vals: number[] = [];", "  const collect = (node: TreeNode | null) => {", "    if (!node) return;", "    collect(node.left); vals.push(node.val); collect(node.right);", "  };", "  collect(root);", "  return vals.length > 0;", "}"]
      },
    },
  },
}

treeSolutions['Lowest Common Ancestor of a Binary Search Tree'] = treeSolutions['Lowest Common Ancestor of a BST']
